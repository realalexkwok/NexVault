<?xml version="1.0" encoding="utf-8"?><!--
   NexVault is a non-custodial wallet: no app data is backed up.
   `android:allowBackup="false"` already disables Auto Backup on API 30 and below; the
   explicit excludes below keep that decision visible and defend against a future flip of
   the flag. Keep in sync with `data_extraction_rules.xml`.
   See https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
