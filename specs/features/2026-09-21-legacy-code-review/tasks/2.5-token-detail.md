# CR 2.5 — Token detail screen

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-25 — sign-off recorded** (open finding 2.5-1 stays in the table as the
> transfer channel for 2.0.6) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.5 Token detail (Phase 0) |
| Prompt | `doc/prompts/15-Task-2.5-Token-Detail-Screen.md` |
| Landing modules | `feature:feature-tokens` (3 files) + `TransactionRepository`/`TransactionRepositoryImpl` + `GetTokenPriceChartUseCase`, `ObserveTokenDetailUseCase`, `GetRecentTokenTransactionsUseCase`, `RefreshTransactionHistoryUseCase` |
| Tests | **0** (feature-tokens) |
| `doc/08` anchor | AC-2.5 (token detail) |

## Measured file inventory (2026-09-21)

- `feature/feature-tokens`: `TokenDetailScreen`, `TokenDetailViewModel`, `TokenDetailUiState`.
- `domain/repository/TransactionRepository.kt`, `data/repository/TransactionRepositoryImpl.kt`
  (send path `DataResult.Error` wrapping `UnsupportedOperationException` at `:22–26`;
  `page`/`pageSize` reserved comment at `:68`).

## Known notes (pre-registered findings — do not re-create)

- **History shortcuts:** `getTransactionHistory` ignores `page`/`pageSize`;
  `updateTransactionStatus` returns the cached entity without consulting the chain — roadmap note
  under 2.5/2.8; the 2.8 planning round must re-read this.
- **Send/Receive buttons** trigger navigation callbacks — empty in `MainScreen` wiring; owner **2.0.3**.
- `TokenIcon`/`SimpleLineChart`/`FormatUtils` live in `core-ui` (verified in 1.2 C10 — no
  feature duplicates).

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :feature:feature-tokens:assembleDebug :data:assembleDebug` →
  **BUILD SUCCESSFUL** (2026-09-25).
- [x] **C2** ✓ pass — route args `contractAddress` (String) / `chainId` (Int) match the
  `SavedStateHandle` keys via `checkNotNull(get<...>)`.
- [x] **C3** ✓ pass — detail shows icon, name, symbol, balance, fiat value (via
  `ObserveTokenDetailUseCase`).
- [x] **C4** ✓ pass — price chart renders with a graceful `token_detail_no_chart_data` branch when
  `chartData.size < 2`.
- [x] **C5** ✓ pass — range FilterChips 1D/7D/1M/1Y drive the chart (days 1/7/30/365).
- [x] **C6** ✓ pass — price + 24h change stats present (PriceChangeChip path).
- [x] **C7** ✓ pass (noted) — Send/Receive buttons visible with typed callbacks; upstream lambdas are
  empty in `MainScreen` — pre-registered **2.0.3**.
- [x] **C8** ✓ pass — recent transactions section renders `uiState.recentTransactions` (limit
  handled in `GetRecentTokenTransactionsUseCase`).
- [x] **C9** ✓ pass — "See All" (`token_detail_see_all`) present, wired to the history navigation
  (placeholder OK).
- [x] **C10** ✓ pass — back button (`onNavigateBack`) wired.
- [x] **C11** ✓ pass — `TokenIcon`/`SimpleLineChart`/`FormatUtils` are core-ui, not duplicated
  (1.2 C10 verified).
- [x] **C12** ✓ pass — `TransactionRepository` in domain; impl in data with the Hilt binding
  (verified across 1.5/1.6).
- [x] **C13** ✓ pass — the 4 use cases exist with `@Inject` + `operator fun invoke`.
- [x] **C14** ✓ pass — KDoc present; `grep 'TODO()'` → **0**.
- [x] **C15** ✓ pass — `refreshTransactionHistoryUseCase(chainId)` wrapped in try/catch (×2) —
  silent-failure per the prompt tip.
- [x] **C16** ✗ finding 2.5-1 — **0 tests** in `feature-tokens` (VM/UiState/chart-state transitions
  uncovered; TC-VM/TC-UI unimplemented).
- [x] **C17** n/a → pre-registered roadmap note — history shortcuts recorded for 2.5/2.8 planning.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2.5-1 | **Medium** | `feature/feature-tokens` | **0 tests** — `TokenDetailViewModel` (arg reading, chart-range switching, refresh tolerance) and `TokenDetailUiState` transitions are uncovered; `doc/07` TC-VM/TC-UI cases have no implementing tests. | `find feature/feature-tokens/src/test -name '*.kt'` → none (2026-09-25); tech-stack §5 "feature-tokens 0 tests" | **2.0.6** (TC traceability) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Sign-off recorded in `../validation.md` — 2026-09-25 (user-confirmed close per Q9; scan T1–T4).
