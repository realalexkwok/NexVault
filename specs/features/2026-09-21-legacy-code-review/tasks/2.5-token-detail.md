# CR 2.5 — Token detail screen

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.5 Token detail (Phase 0) |
| Prompt | `doc/prompts/15-Task-2.5-Token-Detail-Screen.md` |
| Landing modules | `feature:feature-tokens` (3 files) + `TransactionRepository`/`TransactionRepositoryImpl` + `GetTokenPriceChartUseCase`, `ObserveTokenDetailUseCase`, `GetRecentTokenTransactionsUseCase`, `RefreshTransactionHistoryUseCase` |
| Tests | **0** |
| `doc/08` anchor | AC-2.5 (token detail) |

## Measured file inventory (2026-09-21)

- `feature/feature-tokens`: `TokenDetailScreen`, `TokenDetailViewModel`, `TokenDetailUiState`.
- `domain/repository/TransactionRepository.kt`, `data/repository/TransactionRepositoryImpl.kt`
  (send path `UnsupportedOperationException` line 23; gas line 26; `page`/`pageSize` reserved
  comment lines 68–74).

## Known notes (pre-registered findings — do not re-create)

- **History shortcuts:** `getTransactionHistory` ignores `page`/`pageSize`;
  `updateTransactionStatus` returns the cached entity without consulting the chain — roadmap note
  under 2.5/2.8; the 2.8 planning round must re-read this.
- **Send/Receive buttons** trigger navigation callbacks — empty in `MainScreen` wiring; owner **2.0.3**.
- **No tests** — TC-UI/TC-VM cases owned by **2.0.6**.
- `TokenIcon`/`SimpleLineChart`/`FormatUtils` were moved to `core-ui` (prompt tip) — verify no
  copies remain in feature modules (1.2 C10 cross-check).

## Review checklist

- [ ] **C1** `feature-tokens` + `data` compile; app assembles (E1).
- [ ] **C2** Home → Token Detail navigation with arguments matching `SavedStateHandle` lookups
  (AC 4, 20). Verify by: route string vs `savedStateHandle.get<...>` keys (incl. `chainId` type).
- [ ] **C3** Detail shows icon, name, symbol, balance, fiat value (AC 5). Verify by:
  `TokenDetailScreen` + `ObserveTokenDetailUseCase`.
- [ ] **C4** Price chart renders with data points or a graceful "no data" state (AC 6).
  Verify by: chart section + empty-data branch.
- [ ] **C5** Range buttons 1D/7D/1M/1Y switch chart data (AC 7). Verify by:
  `GetTokenPriceChartUseCase` + ViewModel range state.
- [ ] **C6** Price + 24h change stats display correctly (AC 8). Verify by: `TokenDetailUiState`
  fields + formatting via `FormatUtils`.
- [ ] **C7** Send/Receive buttons visible with navigation callbacks (AC 9) — pre-registered **2.0.3**
  for the empty-lambda half; record current wiring.
- [ ] **C8** Recent transactions: up to 5 for this token (AC 10). Verify by:
  `GetRecentTokenTransactionsUseCase` + ViewModel.
- [ ] **C9** "See All" → history navigation (placeholder OK) (AC 11); empty state "No transactions
  yet" (AC 12). Verify by: `TokenDetailScreen`.
- [ ] **C10** Back button returns to Home (AC 13). Verify by: navigation wiring.
- [ ] **C11** `TokenIcon`/`SimpleLineChart`/`FormatUtils` live in `core-ui`, not duplicated
  (AC 14). Verify by: `grep -rln 'fun TokenIcon\|fun SimpleLineChart\|fun formatFiatValue' feature` → empty.
- [ ] **C12** `TransactionRepository` in `domain`; impl in `data` with Hilt binding
  (AC 15–16). Verify by: files + `RepositoryModule`.
- [ ] **C13** Use cases exist (AC 17): `GetTokenPriceChartUseCase`, `ObserveTokenDetailUseCase`,
  `GetRecentTokenTransactionsUseCase`, `RefreshTransactionHistoryUseCase`.
  Verify by: `domain/usecase/token/`.
- [ ] **C14** KDoc on public classes; no `TODO()` (AC 18–19; E7 = 0).
- [ ] **C15** `refreshTransactionHistory` failure is tolerated (prompt tip: try-catch, empty list)
  — verify the screen degrades instead of crashing with placeholder keys. Verify by:
  `TokenDetailViewModel` refresh path.
- [ ] **C16** **0 tests** — record as finding; owning item 2.0.6 or new test item.
- [ ] **C17** History shortcuts — pre-registered roadmap note; record exact current state for the
  developer session / 2.8 planning.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
