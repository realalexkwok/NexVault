# CR 1.4 — DataStore & preferences

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **3 findings recorded 2026-09-22 — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.4 DataStore & preferences (Phase 0) |
| Prompt | `doc/prompts/04-Task-1.4-DataStore-And-Preferences-Settings-User-Preferences-and-Secure-App-State.md` |
| Landing module | `core:core-datastore` — 8 main + 4 test files |
| Tests | **27 pass** (fresh 2026-09-22 rerun) |
| `doc/08` anchor | AC-1.5 (PIN & biometric), AC-1.6 (auto-lock), AC-2.1 (selected-chain persistence), Security cross-cutting |

## Measured file inventory (2026-09-21)

- Main: `di/DataStoreModule`, `model/PreferenceModels`, `model/WalletModels`,
  `preferences/UserPreferencesDataStore`, `security/SecurityPreferencesDataStore`,
  `state/AppStateManager`, `util/DataStoreSerializer`, `wallet/WalletMetadataDataStore`.
- Test: `AppStateManagerTest`, `DataStoreSerializerTest`, `PreferenceModelsTest`, `WalletMetadataTest`.
- Measure command: `find core/core-datastore/src -name '*.kt' | wc -l` → 12.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b observation:** after the broken onboarding run, `security_preferences` held **no
  `password_hash`** — the wallet existed but no PIN was ever set. Root cause lives in 1.7/1.8
  territory; this task reviews the DataStore layer's side (flag + PIN write semantics). Owner: 2.0.2b.
- `is_wallet_set_up` / wallet-created flag semantics: written mid-onboarding (2.0.2b root-cause
  contributor). Owner: 2.0.2b.
- Progressive lockout thresholds are a prompt AC (item 7) — implemented and tested (see C5), but the
  terminal state is broken → finding 1.4-1.

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :core:core-datastore:testDebugUnitTest --rerun-tasks` →
  **BUILD SUCCESSFUL, 27 tests, 0 failures, 0 skipped** (2026-09-22).
- [x] **C2** ✓ pass (noted) — Defaults verified: theme SYSTEM, currency USD, network MAINNET,
  auto-lock FIVE_MINUTES (AC-1.6 default 5 min ✓), notifications/alerts true, require-auth true.
  Note: `themeMode` is never consumed — the UI hardcodes `darkTheme = true` (1.2 C1), so the
  preference is currently inert.
- [x] **C3** ✓ pass — Serialization round-trips preserve fields (`DataStoreSerializerTest` +
  `WalletMetadataTest` green).
- [x] **C4** ✗ finding 1.4-3 — `NetworkType` cannot represent BSC/Polygon (it has the deprecated
  GOERLI instead), so `ChainMapper.chainIdToNetworkType` collapses BSC and Polygon to MAINNET and
  the selected chain does not survive an app restart for 2 of the 4 supported chains.
- [x] **C5** ✗ finding 1.4-1 — Progressive thresholds (5→30s, 8→5m, 10→15m, 15→1h, 20→wipe) are
  implemented and unit-tested (5 tests), but the **terminal state is broken**: `WalletWiped` wipes
  nothing and `UnlockViewModel` navigates to onboarding with `is_wallet_set_up` untouched.
- [x] **C6** ✓ pass — `core-datastore` depends on `core-security` and uses `EncryptionManager`
  (`build.gradle.kts` + `storePasswordHash`).
- [x] **C7** ✓ pass — Three unique DataStore file names: `user_preferences`, `security_preferences`,
  `wallet_metadata`.
- [x] **C8** ✓ pass — PIN hash + salt are stored **KeyStore-encrypted** (Base64 of
  `encryptWithKeystore`), decrypted on read with exception → null. Stronger than the plain
  salted-hash baseline.
- [x] **C9** n/a → pre-registered **2.0.2b** — `is_wallet_set_up` write timing is the 2.0.2b root
  cause; the API surface here (`setWalletSetUp`) is reviewed, not re-created.
- [x] **C10** ✓ pass (cross-ref) — `AppStateManager` exposes `isWalletSetUp`,
  `lastAuthTimestamp`, `autoLockTimeout` flows that drive root routing; the `NexVaultApp`
  `routingKey` consumer is reviewed under 1.8/1.9.
- [x] **C11** ✓ pass — `grep -rnE 'Log\\.|println' core/core-datastore/src/main` → **0**.
- [x] **C12** ✓ pass (noted) — `DataStoreSerializer` catches decode exceptions and returns empty
  lists (corrupt-tolerant); note: a corrupt `wallets` payload is silently treated as "no wallets",
  which masks corruption rather than surfacing it — recorded as a note, not a finding.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1.4-1 | **High** | `AppStateManager.kt:118–131` + `UnlockViewModel.kt:215–225` | The 20-attempt terminal state is broken: `onAuthenticationFailure` returns `WalletWiped` and resets the lockout end time, but **no wallet data is wiped** — `UnlockViewModel` merely shows an error and emits `NavigateToOnboarding` while `is_wallet_set_up` stays true, so the root router will route straight back to unlock. Net effect: after 20 failures the lockout is cancelled (end time 0) and the attacker gets unlimited further attempts with a cosmetic "wallet wiped" message. Adjacent semantic question: the counter is cumulative across lockouts, while prompt 09 AC 9 says "fresh attempt counter" after expiry — owner decision needed (cumulative escalation vs reset). | reads 2026-09-22: `AppStateManager.kt` LOCKOUT_20_ATTEMPTS branch sets `setLockoutEndTime(0L)` and returns without touching wallet stores; `UnlockViewModel.kt:215–225` handles `WalletWiped` with UI + navigation only | **4.7** (security hardening) — **owner decision 2026-09-22: wipe at 20 attempts** (resetApp + WalletStore.wipeAll + KeyStoreManager.deleteAllKeys before NavigateToOnboarding) | **Open** |
| 1.4-2 | **Low** | `AppStateManager.kt:88–99` (`currentWalletDisplayInfo`) | `WalletDisplayInfo` hardcodes `address = ""` and `accountName = "Account 1"` even though `AccountMetadata` carries real addresses and `active_account_index` exists (unused). Currently dead code (no production consumer), but wiring it later would silently show an empty address in the UI. | reads 2026-09-22; `AccountMetadata.address` field exists in `WalletModels.kt`; `grep currentWalletDisplayInfo` → only AppStateManager + tests | proposed new Phase 4 item (display-info cleanup) | **Open** |
| 1.4-3 | **High** | `PreferenceModels.kt:12–18` (`NetworkType`) + `data/mapper/ChainMapper.kt:32–37` | `NetworkType` has MAINNET, GOERLI (deprecated since 2024), SEPOLIA, CUSTOM — no BSC/POLYGON values. `chainIdToNetworkType` therefore maps BSC (56) and Polygon (137) to **MAINNET**, so selecting either chain persists `MAINNET` and the selection does not survive an app restart for 2 of the 4 supported chains — a direct AC-2.1 violation ("Selected chain persists across app restarts"). | reads 2026-09-22: `ChainMapper.kt:33–34` `BSC_MAINNET.chainId -> NetworkType.MAINNET`, `POLYGON_MAINNET.chainId -> NetworkType.MAINNET`; `PreferenceModelsTest` has no BSC/Polygon cases | **2.3** (chain-management CR task; fix by the developer session — pre-registered there) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
