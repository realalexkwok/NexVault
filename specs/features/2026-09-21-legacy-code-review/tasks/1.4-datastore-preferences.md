# CR 1.4 — DataStore & preferences

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.4 DataStore & preferences (Phase 0) |
| Prompt | `doc/prompts/04-Task-1.4-DataStore-And-Preferences-Settings-User-Preferences-and-Secure-App-State.md` |
| Landing module | `core:core-datastore` — 8 main + 4 test files |
| Tests | **27 pass** |
| `doc/08` anchor | AC-1.5 (PIN & biometric), AC-1.6 (auto-lock), Security cross-cutting |

## Measured file inventory (2026-09-21)

- Main: `di/DataStoreModule`, `model/PreferenceModels`, `model/WalletModels`,
  `preferences/UserPreferencesDataStore`, `security/SecurityPreferencesDataStore`,
  `state/AppStateManager`, `util/DataStoreSerializer`, `wallet/WalletMetadataDataStore`.
- Test: `AppStateManagerTest`, `DataStoreSerializerTest`, `PreferenceModelsTest`, `WalletMetadataTest`.
- Measure command: `find core/core-datastore/src -name '*.kt' | wc -l` → 12.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b observation:** after the broken onboarding run, `security_preferences` held **no
  `password_hash`** — the wallet existed but no PIN was ever set. Root cause lives in 1.7/1.8
  territory; this task reviews the DataStore layer's side (flag + PIN write semantics). Owner: 2.0.2b.
- `is_wallet_set_up` / wallet-created flag semantics: written mid-onboarding (2.0.2b root-cause
  contributor). Owner: 2.0.2b.
- Progressive lockout thresholds are a prompt AC (item 7) — verify they exist and are tested.

## Review checklist

- [ ] **C1** Tests pass. Verify by: E2 — `core-datastore` 27 tests green (2026-09-21).
- [ ] **C2** All preference Flows emit correct default values (prompt AC 4). Verify by:
  `UserPreferencesDataStore` defaults + `PreferenceModelsTest`.
- [ ] **C3** Serialization round-trips preserve all fields (prompt AC 5). Verify by:
  `DataStoreSerializer` + `DataStoreSerializerTest`.
- [ ] **C4** Enum values and properties correct (prompt AC 6). Verify by: `PreferenceModels.kt`
  (`NetworkType`, currency, theme, auto-lock duration) + `PreferenceModelsTest`.
- [ ] **C5** Progressive lockout thresholds implemented (prompt AC 7). Verify by:
  `SecurityPreferencesDataStore` (attempt counter → increasing lockout) + its tests.
- [ ] **C6** Depends on `core:core-security` and can access `EncryptionManager` (prompt AC 8;
  tech-stack §2). Verify by: `core/core-datastore/build.gradle.kts`.
- [ ] **C7** Each DataStore has a unique file name (prompt AC 9). Verify by: the four DataStore classes.
- [ ] **C8** PIN stored as a salted hash, never plaintext (doc/08 Security). Verify by:
  `SecurityPreferencesDataStore` write path + `AuthRepositoryImpl` hash flow (cross-check 1.6).
- [ ] **C9** Wallet-created flag write timing — pre-registered **2.0.2b**; reviewed for the DataStore
  API surface only (who writes it, when).
- [ ] **C10** `AppStateManager` flows are the single source for root-navigation state; the
  `NexVaultApp` `routingKey` derivation is reviewed under 1.8/1.9, not here.
- [ ] **C11** No sensitive values logged. Verify by: `grep -nE 'Log\\.|println' core/core-datastore/src/main`.
- [ ] **C12** Serializer handles null/missing gracefully (corrupt-file tolerance).
  Verify by: `DataStoreSerializer` catch path + test.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
