# CR 1.4 — DataStore & preferences

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-22 — sign-off recorded** (open finding 1.4-4 stays in the table as the
> transfer channel for 4.17) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.4 DataStore & preferences (Phase 0) |
| Prompt | `doc/prompts/04-Task-1.4-DataStore-And-Preferences-Settings-User-Preferences-and-Secure-App-State.md` |
| Landing module | `core:core-datastore` — 8 main + 4 test files |
| Tests | **30 pass** (fresh 2026-09-22 rerun; +3 ChainMapperTest live in `data`) |
| `doc/08` anchor | AC-1.5 (PIN & biometric), AC-1.6 (auto-lock), AC-2.1 (selected-chain persistence), Security cross-cutting |

## Measured file inventory (2026-09-21)

- Main: `di/DataStoreModule`, `model/PreferenceModels`, `model/WalletModels`,
  `preferences/UserPreferencesDataStore`, `security/SecurityPreferencesDataStore`,
  `state/AppStateManager`, `util/DataStoreSerializer`, `wallet/WalletMetadataDataStore`.
- Test: `AppStateManagerTest`, `DataStoreSerializerTest`, `PreferenceModelsTest`, `WalletMetadataTest`.
- Measure command: `find core/core-datastore/src -name '*.kt' | wc -l` → 12.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b observation:** after the broken onboarding run, `security_preferences` held **no
  `password_hash`** — the wallet existed but no PIN was ever set. Root cause lives in 1.7/1.8
  territory; this task reviews the DataStore layer's side (flag + PIN write semantics). Owner: 2.0.2b.
- `is_wallet_set_up` / wallet-created flag semantics: written mid-onboarding (2.0.2b root-cause
  contributor). Owner: 2.0.2b.
- Lockout escalation: thresholds implemented and tested; terminal state fixed by the 1.4 fix round
  (wipe at 20, owner decision 2026-09-22).

## Review checklist

- [x] **C1** ✓ pass — fresh `--rerun-tasks` runs: datastore **30 tests, 0 failures, 0 skipped**;
  repo **227 / 0 / 1** (the +3 are the new `ChainMapperTest` cases in `data`).
- [x] **C2** ✓ pass (noted) — Defaults verified (theme SYSTEM, currency USD, network MAINNET,
  auto-lock FIVE_MINUTES, notifications/alerts true, require-auth true). `themeMode` remains
  unconsumed (UI hardcodes dark).
- [x] **C3** ✓ pass — Serialization round-trips + corrupt-tolerance green.
- [x] **C4** ✓ pass (post-fix) — `NetworkType` is now MAINNET/SEPOLIA/BSC/POLYGON (GOERLI/CUSTOM
  dropped); `ChainMapper` round-trips all four (finding 1.4-3, verified fixed).
- [x] **C5** ✓ pass (post-fix) — Progressive thresholds tested; the terminal state now really wipes
  at 20 attempts (owner decision): `wipeAll()` + `deleteAllKeys()` + `resetApp()` before
  `WalletWiped` (finding 1.4-1, verified fixed). Residual: the 15-attempt warning message is a
  hardcoded English string → finding 1.4-4.
- [x] **C6** ✓ pass — depends on `core-security` and uses `EncryptionManager` (now also
  `WalletStore`/`KeyStoreManager` for the wipe — Hilt-resolved, device-verified).
- [x] **C7** ✓ pass — Three unique DataStore file names.
- [x] **C8** ✓ pass — PIN hash + salt stored KeyStore-encrypted.
- [x] **C9** n/a → pre-registered **2.0.2b** (flag write timing reviewed, not re-created).
- [x] **C10** ✓ pass (cross-ref) — AppStateManager flows drive root routing; consumer reviewed under 1.8.
- [x] **C11** ✓ pass — `grep -rnE 'Log\\.|println' core/core-datastore/src/main` → **0**.
- [x] **C12** ✓ pass (noted) — corrupt payloads degrade to empty lists (silent-masking note stands).

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).
> Findings 1.4-1 … 1.4-3 were **fixed by the developer and verified by the reviewer on
2026-09-22 — rows deleted**; evidence stays in `../validation.md` §1.4.

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1.4-4 | **Low** | `AppStateManager.kt:107` | The 15-attempt lockout warning is a hardcoded English string (`"Too many failed attempts. You have 1 hour before wallet wipe warning."`) in `core-datastore`; it flows through `AuthRepositoryImpl` → `UnlockViewModel.handleFailedAttempt(message)` → UI. The 1.2 string sweep only covered `feature/` modules, so this one survived. | reads 2026-09-22: `AppStateManager.kt:107`; `UnlockViewModel.kt:243–246` consumes the message as `String` | **4.17** (string adoption) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Sign-off recorded in `../validation.md` — 2026-09-22 (user-confirmed close per Q9; verification V1–V7).
