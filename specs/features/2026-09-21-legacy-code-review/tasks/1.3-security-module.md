# CR 1.3 — Security module

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.3 Security module (Phase 0) |
| Prompt | `doc/prompts/03-Task-1.3-Security-Module.md` |
| Landing module | `core:core-security` — 10 main + 5 test files |
| Tests | **61, 0 failures, 1 skipped** (post-2.0.1, first-ever run 2026-09-21) |
| `doc/08` anchor | AC-1.5 (PIN), Security cross-cutting rules |

## Measured file inventory (2026-09-21)

- Main: `biometric/BiometricHelper`, `di/SecurityModule`, `encryption/EncryptionManager` +
  `EncryptionModels`, `keystore/KeyStoreManager`, `mnemonic/MnemonicManager`,
  `util/SecurityUtils`, `validation/PasswordValidator`, `wallet/HDKeyManager`, `wallet/WalletStore`.
- Test: `EncryptionManagerTest` 10 (1 skipped), `SecurityUtilsTest` 15, `MnemonicManagerTest` 11,
  `PasswordValidatorTest` 16, `HDKeyManagerTest` 9.
- Measure command: `find core/core-security/src -name '*.kt' | wc -l` → 15.

## Known notes (pre-registered findings — do not re-create)

- **2.0.1 judgment record** (2026-09-21): 8 of 9 compile defects were stale test code; the one real
  production bug was `getWordList()` returning 100 of 2048 BIP-39 words — **fixed** by delegating to
  `MnemonicUtils.getWords()`. A ninth defect (`PasswordValidatorTest` threshold) surfaced on first run.
- **4.11** password-strength ceiling is 75 while `coerceIn(0, 100)` implies 100 — no production caller yet.
- **4.12** `SecureUtils`/`SecurityUtils` two-objects-in-one-file trap (caused three 2.0.1 defects).
- **4.13** keystore-path tests: `testEncryptWithKeystore` is `@Ignore`d and asserts nothing; port to
  `app/src/androidTest` once a device exists.
- `getWordList()` still has **no production caller** (import screen doesn't use it) — 2.0.1 handoff.
- web3j 6.0.0 (G4): no API changes needed; regression-verified. Accepted evidence.

## Review checklist

- [ ] **C1** Suite compiles and passes. Verify by: E2 — `:core:core-security:testDebugUnitTest`
  BUILD SUCCESSFUL, 61/0/1 (2026-09-21; absolute artifact path in `2026-09-20-2.0.0-stabilization/validation.md`).
- [ ] **C2** `getWordList()` returns the canonical 2048-word BIP-39 list (prompt AC 4 territory; 2.0.1 fix).
  Verify by: `MnemonicManager.kt` delegation + `MnemonicManagerTest` (2048, `abandon` first, `zoo` last).
- [ ] **C3** Mnemonic generation/validation consistent for 12/24 words; generated words validate
  against the same list. Verify by: `MnemonicManager.kt` + `MnemonicManagerTest`.
- [ ] **C4** `HDKeyManager` derives correct Ethereum addresses from known test vectors (prompt AC 5).
  Verify by: `HDKeyManagerTest` 9 tests.
- [ ] **C5** `PasswordValidator` validates passwords and PINs (prompt AC 6). Verify by:
  `PasswordValidatorTest` 16 tests; ceiling issue pre-registered **4.11**.
- [ ] **C6** Password-based encrypt/decrypt round-trips (prompt AC 7). Verify by: `EncryptionManagerTest`;
  keystore path pre-registered **4.13**.
- [ ] **C7** Hex conversion + address validation correct (prompt AC 8). Verify by: `SecurityUtilsTest` 15.
- [ ] **C8** No sensitive data in any log statement (prompt AC 10; doc/08 Security). Verify by:
  `grep -nE 'Log\\.|println' core/core-security/src/main`.
- [ ] **C9** Sensitive byte arrays zeroed after use (doc/08 Security; prompt 06 AC 10). Verify by:
  `grep -n 'secureWipe' core/core-security/src/main` at every derive/encrypt call site.
- [ ] **C10** Mnemonic/keys never written plaintext; AndroidKeyStore + Tink AEAD wrapping (mission).
  Verify by: `WalletStore.kt`, `EncryptionManager.kt`, `KeyStoreManager.kt`.
- [ ] **C11** No security-deprecation compiler warnings (prompt AC 9). Verify by: compile output review.
- [ ] **C12** web3j 6 API surface (post-G4) used correctly. Verify by: spot-check
  `MnemonicManager`/`HDKeyManager` imports; G4 regression numbers accepted.
- [ ] **C13** `SecureUtils`/`SecurityUtils` split — pre-registered **4.12**, referenced not re-created.
- [ ] **C14** `getWordList()` production adoption: still unused by the import screen — record the gap
  (2.0.1 handoff) and hand to the owning item.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
