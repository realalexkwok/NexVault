# CR 2.1 — Network module — Retrofit, Web3j, APIs

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.1 Network module (Phase 0) |
| Prompt | `doc/prompts/11-Task-2.1-Network Module-Retrofit-Web3j-APIs.md` |
| Landing module | `core:core-network` — 19 main files |
| Tests | **0** |
| `doc/08` anchor | AC-2.2 (balances), AC-2.8 (history), Security (HTTPS only) |

## Measured file inventory (2026-09-21)

- DTOs: `EtherscanBalanceResponse`, `EtherscanTokenTransferListResponse`,
  `EtherscanTransactionListResponse`, `PriceHistoryResponse`, `TokenPriceResponse`,
  `TokenTransferDto`, `TransactionDto`.
- APIs: `CoinGeckoApi`, `BlockExplorerApi`, `BlockExplorerApiFactory`.
- Config: `ChainConfigProvider` (RPC URLs at lines 43–68), `ChainNetworkConfig`.
- Adapters: `BigDecimalAdapter`, `BigIntegerAdapter`; interceptors: `CacheControlInterceptor`,
  `CoinGeckoApiKeyInterceptor`; `web3/Web3jProvider`; `util/AddressValidator`; `di/NetworkModule`.
- In `app`: `di/AppNetworkModule` (BuildConfig keys).
- Measure command: `find core/core-network/src -name '*.kt' | wc -l` → 19.

## Known notes (pre-registered findings — do not re-create)

- **API keys are `your_key_here` placeholders** (all five) — degradation at runtime is expected;
  the per-screen key-absent behaviour decision is owned by **2.0.4**. No faking data (mission).
- **No tests** — `doc/07` TC-NET (3 cases) has no implementation; traceability owned by **2.0.6**.
- web3j 6.0.0 (G4): no source changes needed; Jackson packaging exclude added in `app` —
  accepted evidence (stabilization `validation.md`).
- `ChainConfigProvider` holds chain RPC config; `domain/Chain.kt` also carries `rpcUrl` —
  duplication reviewed under 2.3 C10.

## Review checklist

- [ ] **C1** Module compiles; app assembles. Verify by: E1 (PASS) + `:core:core-network:assembleDebug`.
- [ ] **C2** Every DTO has `@JsonClass(generateAdapter = true)` (prompt AC 3). Verify by:
  `grep -c '@JsonClass' core/core-network/src/main/java/.../dto` → 7.
- [ ] **C3** `CoinGeckoApi`: `getTokenPrices()` + `getPriceHistory()` (AC 4).
  Verify by: read `CoinGeckoApi.kt`.
- [ ] **C4** `BlockExplorerApi`: `getTransactions()`, `getTokenTransfers()`, `getBalance()`
  (AC 5); `BlockExplorerApiFactory` builds per-chain instances (AC 6). Verify by: read both files.
- [ ] **C5** `Web3jProvider` caches instances by chain ID in a `ConcurrentHashMap` (AC 7).
  Verify by: `Web3jProvider.kt`.
- [ ] **C6** `ChainConfigProvider` RPC URLs for chains 1, 11155111, 56, 137 (AC 8).
  Verify by: lines 43–68 (Infura/Alchemy templated with keys, BSC/Polygon public).
- [ ] **C7** `NetworkModule` provides Moshi, OkHttpClient, CoinGeckoApi via Hilt (AC 9);
  `AppNetworkModule` provides the four `@Named` key strings from BuildConfig (AC 10);
  `app/build.gradle.kts` has `buildConfigField` for all keys (AC 11). Verify by: read all three.
- [ ] **C8** `BigDecimalAdapter` handles JSON numbers **and** strings (AC 12); `BigIntegerAdapter`
  handles decimal and `0x`-prefixed hex (AC 13). Verify by: read both adapters.
- [ ] **C9** `AddressValidator.isValid()` true for valid 42-char checksummed addresses, false
  otherwise (AC 14). Verify by: read + spot-check against `Keys.toChecksumAddress` semantics.
- [ ] **C10** OkHttp: 30s timeouts, 10MB cache, logging interceptor (AC 15).
  Verify by: `NetworkModule.kt` OkHttpClient builder.
- [ ] **C11** No hardcoded API keys in source (AC 18); keys flow from BuildConfig only.
  Verify by: `grep -rnE '(api[_-]?key|token) = "[A-Za-z0-9]{8,}"' core app --include='*.kt'`
  (placeholders `your_key_here` are the 2.0.4 pre-registered state).
- [ ] **C12** KDoc on all public classes (AC 17). Verify by: spot-read.
- [ ] **C13** HTTPS-only endpoints (doc/08 Security). Verify by: all URLs in
  `ChainConfigProvider`/API base URLs start `https://`.
- [ ] **C14** **0 tests** — record as finding (Medium/High); owning item 2.0.6 (TC-NET mapping) or a
  new test item per owner decision at sign-off.
- [ ] **C15** CoinGecko interceptor sends the key header only when the key is real; absent-key
  behaviour is the 2.0.4 decision. Verify by: `CoinGeckoApiKeyInterceptor.kt`.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
