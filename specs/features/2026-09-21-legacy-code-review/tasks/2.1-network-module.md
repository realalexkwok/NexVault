# CR 2.1 — Network module — Retrofit, Web3j, APIs

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-24 — sign-off recorded** (open finding 2.1-1 stays in the table as the
> transfer channel for 2.0.6) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.1 Network module (Phase 0) |
| Prompt | `doc/prompts/11-Task-2.1-Network Module-Retrofit-Web3j-APIs.md` |
| Landing module | `core:core-network` — 19 main files |
| Tests | **0** |
| `doc/08` anchor | AC-2.2 (balances), AC-2.8 (history), Security (HTTPS only) |

## Measured file inventory (2026-09-21)

- DTOs: `EtherscanBalanceResponse`, `EtherscanTokenTransferListResponse`,
  `EtherscanTransactionListResponse`, `PriceHistoryResponse`, `TokenPriceResponse`,
  `TokenTransferDto`, `TransactionDto`.
- APIs: `CoinGeckoApi`, `BlockExplorerApi`, `BlockExplorerApiFactory`.
- Config: `ChainConfigProvider` (RPC URLs at lines 43–68), `ChainNetworkConfig`.
- Adapters: `BigDecimalAdapter`, `BigIntegerAdapter`; interceptors: `CacheControlInterceptor`,
  `CoinGeckoApiKeyInterceptor`; `web3/Web3jProvider`; `util/AddressValidator`; `di/NetworkModule`.
- In `app`: `di/AppNetworkModule` (BuildConfig keys).
- Measure command: `find core/core-network/src -name '*.kt' | wc -l` → 19.

## Known notes (pre-registered findings — do not re-create)

- **API keys are `your_key_here` placeholders** (all five) — degradation at runtime is expected;
  the per-screen key-absent behaviour decision is owned by **2.0.4**. No faking data (mission).
- web3j 6.0.0 (G4): no source changes needed; Jackson packaging exclude added in `app` —
  accepted evidence.
- `ChainConfigProvider` holds chain RPC config; `domain/Chain.kt` `rpcUrl` incompleteness —
  reviewed under 2.3 C10.

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :core:core-network:assembleDebug` → **BUILD SUCCESSFUL** (2026-09-24).
- [x] **C2** ✓ pass — all **7** DTOs carry `@JsonClass(generateAdapter = true)` (`grep -rL` empty).
- [x] **C3** ✓ pass — `CoinGeckoApi`: `getTokenPrices` (`simple/price`) + `getPriceHistory`
  (`coins/{id}/market_chart`).
- [x] **C4** ✓ pass — `BlockExplorerApi`: `getTransactions`/`getTokenTransfers`/`getBalance`;
  `BlockExplorerApiFactory` builds per-chain instances (verified in the 1.1 scan).
- [x] **C5** ✓ pass — `Web3jProvider` caches `ConcurrentHashMap<Int, Web3j>` keyed by chain ID.
- [x] **C6** ✓ pass — `ChainConfigProvider` RPC URLs for chains 1/11155111/56/137 (lines 43–68;
  Infura/Alchemy templated with keys, BSC/Polygon public).
- [x] **C7** ✓ pass — `NetworkModule` provides Moshi/OkHttp/CoinGeckoApi via Hilt;
  `AppNetworkModule` provides the `@Named` key strings from BuildConfig (verified in 1.1).
- [x] **C8** ✓ pass — `BigDecimalAdapter` handles JSON numbers + strings; `BigIntegerAdapter`
  handles numbers, decimal strings, and `0x`/`0X`-prefixed hex (radix-16).
- [x] **C9** ✓ pass — `AddressValidator` regex `^0x[0-9a-fA-F]{40}$` (42-char) ✓.
- [x] **C10** ✓ pass — OkHttp: 10 MB cache, 30 s timeouts, `HttpLoggingInterceptor(BASIC)` +
  `CacheControlInterceptor`.
- [x] **C11** ✓ pass — no hardcoded API keys in source; keys flow from BuildConfig via
  `AppNetworkModule` (placeholders are the 2.0.4 pre-registered state).
- [x] **C12** ✓ pass — KDoc present on the public classes spot-read.
- [x] **C13** ✓ pass — `grep 'http://'` in `core-network/src/main` → **0** plain-HTTP endpoints.
- [x] **C14** ✗ finding 2.1-1 — **0 tests** in `core-network`; `doc/07` TC-NET (3 cases) has no
  implementation.
- [x] **C15** ✓ pass (pre-registered 2.0.4) — `CoinGeckoApiKeyInterceptor` attaches
  `x-cg-demo-api-key` **only when the key is non-blank**; absent-key behavior is the 2.0.4 decision.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2.1-1 | **Medium** | `core/core-network` | **0 tests** — no unit tests for the adapters, AddressValidator, ChainConfigProvider, interceptors, or DTO parsing. `doc/07` TC-NET (3 cases) has no implementing tests; placeholder API keys mean integration tests can't run yet either. | `find core/core-network/src/test -name '*.kt'` → none (2026-09-24); tech-stack §5 "core-network 0 tests" | **2.0.6** (TC traceability) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Sign-off recorded in `../validation.md` — 2026-09-24 (user-confirmed close per Q9; scan W1–W4).
