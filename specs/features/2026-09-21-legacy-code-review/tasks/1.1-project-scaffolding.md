# CR 1.1 — Project scaffolding

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-21 — owner sign-off recorded** (open finding 1.1-3 handed to the
> developer session's next round) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.1 Project scaffolding (Phase 0) |
| Prompt | `doc/prompts/01-Task-1.1-Project-Scaffolding.md` |
| Landing modules | whole build: root `build.gradle.kts`, `settings.gradle.kts`, `gradle/libs.versions.toml`, `gradle.properties`, wrapper, `config/detekt/`, 20 module `build.gradle.kts` files + `app` entry classes |
| Tests | `app/src/test` 1 (template stub), `app/src/androidTest` 2 (`ExampleInstrumentedTest` + `BackupPolicyTest` — the latter added by the developer for finding 1.1-1, 2026-09-21) |
| `doc/08` anchor | AC-1.1 |

## Measured file inventory (2026-09-21)

- Build/config: `settings.gradle.kts` (20 modules), root `build.gradle.kts`, `gradle/libs.versions.toml`,
  `gradle.properties`, `gradle/wrapper/gradle-wrapper.properties` (+ `distributionSha256Sum`),
  `config/detekt/detekt.yml`, 20 module `build.gradle.kts` files. No `build-logic/`, no `.editorconfig`.
- `app` entry: `app/src/main/AndroidManifest.xml`, `NexVaultApplication.kt`, `MainActivity.kt`
  (reworked by 2.0.2; `NexVaultApp.kt` added by 1.8/1.9).
- Tests: `app/src/test/java/com/nexvault/wallet/ExampleUnitTest.kt`,
  `app/src/androidTest/java/com/nexvault/wallet/ExampleInstrumentedTest.kt`,
  `app/src/androidTest/java/com/nexvault/wallet/BackupPolicyTest.kt` (new, developer, 2026-09-21).
- Measure command: `find . -name 'build.gradle.kts' -not -path '*/build/*' | wc -l` → 21 (root + 20 modules).

## Known notes (pre-registered findings — do not re-create)

- **Manifest entry point (FIXED, 2.0.2).** The app shipped with no `<activity>` and no
  `android:name` on `<application>`: it could never be launched and Hilt was never registered.
  Fixed 2026-09-21 (`.MainActivity` MAIN/LAUNCHER + `.NexVaultApplication`). Owner: 2.0.2 (done).
- **Quality gates red.** Fresh 2026-09-21 numbers in `validation.md` (A2/A3); remediation owned by **2.0.5**.
- **No convention plugins.** All 20 modules duplicate their config; `build-logic/` absent — owned by **4.1**.
- **Gradle 10 deprecation** (`ReportingExtension.file`, raised from `build.gradle.kts:16` by the
  detekt plugin) — owned by **4.9**.
- **Spotless is a no-op** (applied, unconfigured) — must never be cited as quality evidence.
- **Catalog hygiene (G5, applied).** `shimmer` pinned to the real 0.5.0, `zxing` library entry added,
  dead `composeOptions` blocks removed — verify current tree matches.
- **Kotlin ↔ KSP lockstep**: Kotlin 2.3.10, KSP 2.3.12 (no 2.4.x KSP exists) — `tech-stack.md` §4.
- **AGP 9.4.1 requires Gradle 9.6.0** — `2026-09-20-2.0.0-stabilization/validation.md`.

## Review checklist

- [x] **C1** ✓ pass — Version catalog is the single source of dependency versions. The only literal
  version strings outside the catalog: a **commented-out** Netty resolution block
  (`app/build.gradle.kts:7–13`) and the app's own `versionName = "1.0"` (`app/build.gradle.kts:30`).
  Neither is a dependency declaration. Evidence:
  `grep -nE '"[0-9]+\.[0-9]+' --include='build.gradle.kts' -r app core data domain feature`.
- [x] **C2** ✓ pass — `settings.gradle.kts` declares exactly the 20 modules of `tech-stack.md` §2
  (1 app + 6 core + domain + data + 11 features). Dependency edges spot-verified in 9 module build
  files (feature → domain/core-common/core-ui; data → domain + five `core:*`, no core-ui; core-ui →
  domain; etc.). The §2 wording drift (finding 1.1-2) is **fixed and verified 2026-09-21** — see
  `validation.md` V4.
- [x] **C3** ✓ pass — Root `build.gradle.kts` applies detekt, ktlint and spotless to **all**
  subprojects (`subprojects { apply(...) }`), and declares every plugin `apply false` via catalog
  aliases. No module opts out.
- [x] **C4** ✓ pass — `config/detekt/detekt.yml`: `maxIssues: 0`, `maxLineLength: 120`,
  `TooManyFunctions thresholdInFiles: 11`, `weights.complexity: 2` — matches `tech-stack.md` §6.
- [x] **C5** ✓ pass — `app/build.gradle.kts`: `applicationId com.nexvault.wallet`, `minSdk 26`,
  `compileSdk release(37) { minorApiLevel = 2 }` (= 37.2), `targetSdk 37`, Java/JVM 17,
  `buildConfig = true`, five `buildConfigField` entries for the API keys, debug
  `applicationIdSuffix ".debug"`, release minify enabled, packaging excludes incl. the Jackson
  license collision (G4).
- [x] **C6** ✓ pass — Entry point: `.MainActivity` with `MAIN`/`LAUNCHER` intent filter and
  `.NexVaultApplication` on `<application>` (the 2.0.2 fix). Backup posture (former finding 1.1-1):
  **fixed by the developer and verified 2026-09-21** — `android:allowBackup="false"`
  (`AndroidManifest.xml:7`) plus explicit all-domain excludes in `backup_rules.xml` and both
  `cloud-backup` and `device-transfer` sections of `data_extraction_rules.xml`; evidence in
  `validation.md` V1–V3, V5–V8.
- [x] **C7** ✓ pass — `./gradlew :app:assembleDebug` **PASS** (fresh `--rerun-tasks` run,
  2026-09-21); artifact: `app/build/outputs/apk/debug/app-debug.apk` (45,026,282 bytes, 2026-09-21
  18:58). Evidence `validation.md` V5.
- [x] **C8** ✓ pass — `grep -rnE 'TODO|FIXME' --include='*.kt' app core data domain feature` → **0**.
  The template `<!-- TODO -->` comments that lived in the two backup-rule XMLs were **removed** by the
  1.1-1 fix (verified: both files now carry real rationale comments).
- [x] **C9** ✓ pass (measured + attributed) — fresh 2026-09-21: detekt **FAIL** (10 tasks, 91 weighted
  issues) and ktlint **FAIL** (39 tasks). Remediation stays with **2.0.5** (pre-registered) — not
  re-created here.
- [x] **C10** ✓ pass — `app` depends on all 20 modules (verified in `app/build.gradle.kts`); app
  sources hold only entry points, DI (`AppNetworkModule`), root navigation and the UI shell — no
  business logic (2.0.3's empty callbacks are pre-registered under 1.9). The 7 future feature modules
  (send/receive/history/dapp/nft/swap/settings) and `core-common` are declared with **0** Kotlin
  files — expected scaffolding; `core-common`'s fate is pre-registered **4.2**.
- [x] **C11** n/a — prompt AC "launches on emulator" superseded: the first real run was on a physical
  Pixel 6a (2.0.2), which is how the C6 entry-point defect surfaced.
- [x] **C12** ✓ pass — wrapper pinned to `gradle-9.6.0-bin.zip` with `distributionSha256Sum` and
  `validateDistributionUrl=true`; `gradle.properties` sets `-Xmx2048m` and
  `kotlin.code.style=official`.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).
> Findings 1.1-1 and 1.1-2 were **fixed by the developer and verified by the reviewer on
2026-09-21 — rows deleted**; the verification evidence stays in `../validation.md` §1.1.

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1.1-3 | **Low** (test-side) | `app/src/androidTest/java/com/nexvault/wallet/ExampleInstrumentedTest.kt:22` | The template instrumented test asserts `assertEquals("com.nexvault.wallet", appContext.packageName)`, but the debug build's `applicationId` is `com.nexvault.wallet.debug` (`applicationIdSuffix ".debug"`, `app/build.gradle.kts`). The test therefore **fails on every debug run**. Fix: assert against `BuildConfig.APPLICATION_ID`. Left untouched by the developer (reviewer scope note). | Fresh device run 2026-09-21: `ComparisonFailure: expected:<com.nexvault.wallet[]> but was:<com.nexvault.wallet[.debug]>` at `ExampleInstrumentedTest.kt:22` — see `validation.md` V6 | Developer session (next fix round) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Owner sign-off recorded in `../validation.md` — 2026-09-21 (reviewer close per the owner's branch sign-off instruction; fixes verified V1–V8 and confirmed by the user).
