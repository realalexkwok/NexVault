# CR 2.4 — Home dashboard — balances, chart, add token

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.4 Home dashboard (Phase 0) |
| Prompt | `doc/prompts/14-Task-2.4-Home-Dashboard-Token-Balances-Portfolio-Chart-And-Add-Token.md` |
| Landing modules | `feature:feature-home` (4 files) + domain token use cases (8) + `TokenRepository`/`TokenRepositoryImpl` + `TokenDao` additions |
| Tests | **0** |
| `doc/08` anchor | AC-2.2 (balances), AC-2.3 (chart), AC-2.4 (add token) |

## Measured file inventory (2026-09-21)

- `feature/feature-home`: `HomeScreen`, `HomeViewModel`, `HomeUiState`, `AddTokenDialog`.
- `domain/usecase/token/` 8: `AddCustomTokenUseCase`, `GetPortfolioUseCase`,
  `GetPriceHistoryUseCase`, `GetRecentTokenTransactionsUseCase` (2.5),
  `GetTokenPriceChartUseCase` (2.5), `ObserveTokenDetailUseCase` (2.5),
  `RefreshBalancesUseCase`, `RefreshTransactionHistoryUseCase` (2.5).
- `domain/repository/TokenRepository.kt` (incl. `seedDefaultTokens` at lines 80–83),
  `data/repository/TokenRepositoryImpl.kt` (`seedDefaultTokens` at line 306).

## Known notes (pre-registered findings — do not re-create)

- **detekt `TooManyFunctions`:** `TokenRepositoryImpl` 19/11 — owner **2.0.5**.
- **2.9 reconciliation:** default token seeding on wallet creation exists
  (`WalletRepositoryImpl`:93/154/210 → `seedDefaultTokensForSupportedChains()` line 226) while
  roadmap 2.9 is `[ ]` — record exactly what exists; chain-switch seeding is the open half.
- **Quick actions** Send/Receive/Swap and empty callbacks — owner **2.0.3** (reviewed in 1.9 too).
- **API keys placeholder** → balance/price screens degrade; behaviour decision owned by **2.0.4**.
- **No tests** — TC-VM/TC-UI cases owned by **2.0.6**.

## Review checklist

- [ ] **C1** `feature-home` + `data` compile; app assembles (E1).
- [ ] **C2** Total portfolio balance in USD (AC 4). Verify by: `GetPortfolioUseCase` +
  `HomeViewModel`.
- [ ] **C3** Portfolio chart with 1D/7D/1M/3M/ALL range buttons (AC 5; doc/08 AC-2.3).
  Verify by: `HomeScreen` chart section + `SimpleLineChart` usage.
- [ ] **C4** Token list: native coin per chain, balance + fiat value, 24h change, sorted by fiat
  value desc (AC 6–9; AC-2.2). Verify by: `HomeViewModel` + `TokenDao.getTokensByChain` order.
- [ ] **C5** Pull-to-refresh reloads balances (AC 10). Verify by: `HomeScreen` refresh wiring.
- [ ] **C6** Chain selector in the header; switching updates the list (AC 11–12; AC-2.1).
  Verify by: `HomeScreen` + `ChainSelectorDropdown` + `HomeViewModel` chain flow.
- [ ] **C7** Add Token dialog: valid address → token added; invalid → error in dialog
  (AC 13–15; AC-2.4). Verify by: `AddTokenDialog.kt` + `AddCustomTokenUseCase`.
- [ ] **C8** Quick actions visible (AC 16) — callbacks empty; pre-registered **2.0.3**.
- [ ] **C9** Token row → detail navigation callback (AC 17) — Home → TokenDetail wired
  (roadmap 2.0.3 note). Verify by: `MainScreen`/navigation.
- [ ] **C10** Loading shimmer, empty state, network-error message without crash (AC 18–20).
  Verify by: `HomeScreen` state branches.
- [ ] **C11** `TokenRepository` in `domain`; impl in `data` with Hilt binding (AC 21–22).
  Verify by: files + `RepositoryModule`.
- [ ] **C12** Use cases exist: `GetPortfolioUseCase`, `RefreshBalancesUseCase`,
  `AddCustomTokenUseCase`, `GetPriceHistoryUseCase` (AC 23). Verify by: `usecase/token/`.
- [ ] **C13** Default-token seeding: wallet-creation path exists; **chain-switch path** — check
  whether switching chains seeds (`SetSelectedChainUseCase` / `RefreshBalancesUseCase`).
  Record for 2.9 re-planning.
- [ ] **C14** Blocking Web3j calls wrapped in `withContext(Dispatchers.IO)` (prompt tip).
  Verify by: `grep -n 'withContext' data/src/main/.../TokenRepositoryImpl.kt`.
- [ ] **C15** KDoc on public classes; no `TODO()` (AC 25–26; E7 = 0).
- [ ] **C16** **0 tests** — record as finding; owning item 2.0.6 or new test item.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
