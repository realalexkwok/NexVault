# CR 2.4 — Home dashboard — balances, chart, add token

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-25 — sign-off recorded** (open finding 2.4-1 stays in the table as the
> transfer channel for 2.0.6) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.4 Home dashboard (Phase 0) |
| Prompt | `doc/prompts/14-Task-2.4-Home-Dashboard-Token-Balances-Portfolio-Chart-And-Add-Token.md` |
| Landing modules | `feature:feature-home` (4 files) + domain token use cases (8) + `TokenRepository`/`TokenRepositoryImpl` + `TokenDao` additions |
| Tests | **0** (feature-home) |
| `doc/08` anchor | AC-2.2 (balances), AC-2.3 (chart), AC-2.4 (add token) |

## Measured file inventory (2026-09-21)

- `feature/feature-home`: `HomeScreen`, `HomeViewModel`, `HomeUiState`, `AddTokenDialog`.
- `domain/usecase/token/` 8: `AddCustomTokenUseCase`, `GetPortfolioUseCase`,
  `GetPriceHistoryUseCase`, `GetRecentTokenTransactionsUseCase` (2.5),
  `GetTokenPriceChartUseCase` (2.5), `ObserveTokenDetailUseCase` (2.5),
  `RefreshBalancesUseCase`, `RefreshTransactionHistoryUseCase` (2.5).
- `domain/repository/TokenRepository.kt` (incl. `seedDefaultTokens`), `data/repository/TokenRepositoryImpl.kt`.

## Known notes (pre-registered findings — do not re-create)

- **detekt `TooManyFunctions`:** `TokenRepositoryImpl` 19/11 — owner **2.0.5**.
- **2.9 reconciliation (resolved by the 1.5 fix round):** default-token seeding now covers wallet
  creation AND chain switch (`RefreshBalancesUseCase`); roadmap 2.9 flipped `[~]`.
- **Quick actions** Send/Receive/Swap empty callbacks in `MainScreen` — owner **2.0.3** (reviewed in 1.9).
- **API keys placeholder** → balance/price screens degrade; behaviour decision owned by **2.0.4**.
- `GetPortfolioUseCase`/`RefreshBalancesUseCase` domain logic is tested (added by the 1.5 fix
  round); the **feature-home UI/VM layer** has no tests → finding 2.4-1.

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :feature:feature-home:assembleDebug :data:assembleDebug` →
  **BUILD SUCCESSFUL** (2026-09-25).
- [x] **C2** ✓ pass — total portfolio balance in USD via `GetPortfolioUseCase` →
  `totalFiatValue` (HomeUiState).
- [x] **C3** ✓ pass — chart range FilterChips 1D/7D/1M/3M/ALL (days 1/7/30/90/365) driving
  `chartDays`.
- [x] **C4** ✓ pass — token list renders balance + fiat + 24h change; sort by fiat desc comes from
  `TokenDao` (verified in 2.2).
- [x] **C5** ✓ pass — `PullToRefreshBox` wired to `viewModel.onRefresh()`.
- [x] **C6** ✓ pass — `ChainSelectorHeader` + `ChainSelectorDropdown`; switching dispatches
  `SetSelectedChainUseCase` and re-drives the portfolio flow.
- [x] **C7** ✓ pass — `AddTokenDialog` with `errorRes` support + `onConfirm → onAddCustomToken`;
  invalid-address error path present in the VM.
- [x] **C8** n/a → pre-registered **2.0.3** — Send/Receive/Swap quick actions render, their callbacks
  are the empty lambdas recorded under 1.9.
- [x] **C9** ✓ pass — token row → `onNavigateToTokenDetail(contractAddress, chainId)` wired in
  `MainScreen`.
- [x] **C10** ✓ pass — shimmer loading rows (×5), empty state (`home_empty_tokens`), error snackbar,
  chart no-data branch.
- [x] **C11** ✓ pass — `TokenRepository` in domain; `TokenRepositoryImpl` in data with the Hilt
  binding (verified across 1.5/1.6).
- [x] **C12** ✓ pass — the 4 required use cases (+4 more) all `@Inject` + `operator fun invoke`.
- [x] **C13** ✓ pass — seeding on wallet creation AND chain switch (1.5 fix round; roadmap 2.9 `[~]`).
- [x] **C14** ✓ pass — blocking Web3j calls wrapped in `withContext(Dispatchers.IO)` inside
  `TokenRepositoryImpl` (verified in the 1.6 read).
- [x] **C15** ✓ pass — `grep 'TODO()'` → **0**; KDoc blocks present.
- [x] **C16** ✗ finding 2.4-1 — **0 tests** in `feature-home` (VM/UiState/AddTokenDialog uncovered;
  the domain token use cases are covered post-1.5).

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2.4-1 | **Medium** | `feature/feature-home` | **0 tests** — `HomeViewModel`, `HomeUiState` transitions and `AddTokenDialog` validation are uncovered; `doc/07` TC-VM/TC-UI cases have no implementing tests. The domain-layer token use cases got tests in the 1.5 round, but the home VM layer did not. | `find feature/feature-home/src/test -name '*.kt'` → none (2026-09-25); tech-stack §5 "feature-home 0 tests" | **2.0.6** (TC traceability) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Sign-off recorded in `../validation.md` — 2026-09-25 (user-confirmed close per Q9; scan H1–H4).
