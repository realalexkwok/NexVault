# CR 1.6 — Data layer — repository implementations

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.6 Data layer (Phase 0) |
| Prompt | `doc/prompts/06-Task-1.6-Data Layer-Repository-Implementations.md` |
| Landing module | `data` — 5 repository impls + 5 mappers + `RepositoryModule` + 3 test files |
| Tests | **27 pass** |
| `doc/08` anchor | AC-1.3–1.5 (creation/import/PIN storage paths) |

## Measured file inventory (2026-09-21)

- Impls: `AuthRepositoryImpl`, `ChainRepositoryImpl`, `TokenRepositoryImpl` (2.4),
  `TransactionRepositoryImpl` (2.5), `WalletRepositoryImpl`.
- Mappers: `AuthMapper`, `ChainMapper`, `TokenMapper`, `TransactionMapper`, `WalletMapper`.
- DI: `di/RepositoryModule`.
- Tests: `AuthRepositoryImplTest`, `ChainRepositoryImplTest`, `WalletRepositoryImplTest` (27 total).
- Measure command: `find data/src -name '*.kt' | wc -l` → 14.

## Known notes (pre-registered findings — do not re-create)

- **2.6 entry condition:** `TransactionRepositoryImpl` throws `UnsupportedOperationException` for
  the send path (`TransactionRepositoryImpl.kt:23`) and gas estimation (line 26). Owner: **2.6**.
- **History shortcuts:** `getTransactionHistory` ignores `page`/`pageSize` (reserved comment at
  lines 68–74); `updateTransactionStatus` returns the cached entity without consulting the chain.
  Owner: roadmap note under 2.5/2.8.
- **detekt `TooManyFunctions`:** `TokenRepositoryImpl` 19/11, `WalletRepositoryImpl` 14/11,
  `AuthRepositoryImpl` 13/11. Owner: **2.0.5**.
- **2.0.2b contributor:** `WalletRepositoryImpl.createWallet` writes both the wallet and the
  wallet-created flag — the flag flip mid-onboarding is the root cause recorded in 2.0.2b.
- Default token seeding exists in `WalletRepositoryImpl.seedDefaultTokensForSupportedChains()`
  (lines 93/154/210 → 226–229) — reconcile with roadmap 2.9 (see 1.5 C10).

## Review checklist

- [ ] **C1** Tests pass. Verify by: E2 — `data` 27 tests green (2026-09-21).
- [ ] **C2** `WalletRepositoryImpl` orchestration: mnemonic generation → derivation → encryption →
  storage (prompt AC 4). Verify by: read `WalletRepositoryImpl` create/import paths
  (seed derive at lines 57–62, 119–123, 289–293; `secureWipe` after use).
- [ ] **C3** `AuthRepositoryImpl` hashes PINs and handles the lockout flow (prompt AC 5).
  Verify by: read `AuthRepositoryImpl` + `AuthRepositoryImplTest`.
- [ ] **C4** `ChainRepositoryImpl` returns correct chains and handles selection (prompt AC 6).
  Verify by: `ChainRepositoryImpl` + `ChainRepositoryImplTest`.
- [ ] **C5** `RepositoryModule` binds all implementations to their interfaces (prompt AC 7 —
  now 5 bindings incl. Token/Transaction). Verify by: read `di/RepositoryModule.kt`.
- [ ] **C6** No `TODO()` calls (prompt AC 8; E7 = 0). Verify by: `grep -rn 'TODO()' data/src`.
- [ ] **C7** All placeholder types replaced with real core-module types (prompt AC 9).
  Verify by: spot-read mappers + impls.
- [ ] **C8** Sensitive byte arrays zeroed after use (prompt AC 10; doc/08 Security). Verify by:
  `grep -n 'secureWipe' data/src/main` at every derive/sign site.
- [ ] **C9** Mappers map every DTO/model field without silent loss. Verify by: read the 5 mappers
  against `domain` models and `core-network` DTOs.
- [ ] **C10** `DataResult`/`Flow` convention: repository methods return `DataResult<T>` or
  `Flow<T>`, never throw to callers (tech-stack §3). Verify by: read impls for bare `throw`
  outside `UnsupportedOperationException`/mapping.
- [ ] **C11** Write-path shortcuts — pre-registered **2.6** + 2.5/2.8 notes; record current state
  exactly for the developer session.
- [ ] **C12** `data` dependency edges: `domain` + all six `core:*` (tech-stack §2).
  Verify by: `data/build.gradle.kts`.
- [ ] **C13** `TooManyFunctions` offenders — pre-registered **2.0.5**; no re-creation.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
