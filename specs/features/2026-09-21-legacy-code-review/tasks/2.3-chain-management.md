# CR 2.3 — Chain management — repository, use cases, selector UI

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.3 Chain management (Phase 0) |
| Prompt | `doc/prompts/13-Task-2.3-Chain-Management-Repository-Use-Cases-Chain-Selector-UI.md` |
| Landing modules | `domain` (Chain model, `ChainRepository`, 3 use cases) · `data` (`ChainRepositoryImpl`, `ChainMapper`) · `core:core-ui` (`ChainSelectorDropdown`, `ChainBadge`, `ChainUi`, `ChainUiMapper`, `ChainIconMapper`, 4 drawables) |
| Tests | **0** |
| `doc/08` anchor | AC-2.1 (multi-chain) |

## Measured file inventory (2026-09-21)

- `domain/model/chain/Chain.kt` (incl. `SupportedChains`), `domain/repository/ChainRepository.kt`,
  `usecase/chain/`: `GetSupportedChainsUseCase`, `GetSelectedChainUseCase`, `SetSelectedChainUseCase`.
- `data/repository/ChainRepositoryImpl.kt`, `data/mapper/ChainMapper.kt`.
- `core/core-ui`: `components/ChainBadge`, `components/ChainSelectorDropdown`, `components/ChainUi`,
  `mapper/ChainUiMapper`, `util/ChainIconMapper`; drawables `ic_chain_ethereum`,
  `ic_chain_sepolia`, `ic_chain_bsc`, `ic_chain_polygon` (verified present 2026-09-21).

## Known notes (pre-registered findings — do not re-create)

- **No tests** — chain use cases + mapper uncovered; TC-UC cases owned by **2.0.6**.
- **Chain config in two places:** `domain/Chain.kt` carries `rpcUrl` per chain (e.g. line 55),
  and `core-network/ChainConfigProvider` holds the RPC config used by `Web3jProvider`/
  `BlockExplorerApiFactory`. Prompt AC 21 demanded no duplicate chain configuration — the review
  must judge whether identity-in-domain + RPC-in-network is an acceptable split or a real duplication.
- Prompt AC 10 said `setSelectedChain` throws `IllegalArgumentException` for unsupported IDs; the
  implementation returns `DataResult.Error` — a deviation to judge against the `DataResult`
  convention (tech-stack §3: repository methods never throw).

## Review checklist

- [ ] **C1** `ChainRepository` interface in `domain` with all methods (AC 5);
  `ChainRepositoryImpl` in `data` with the 4 hardcoded chains (AC 6–7: 1, 11155111, 56, 137).
  Verify by: read both files + `SupportedChains`.
- [ ] **C2** `getSelectedChain()` emits the chain matching `selectedChainId` from DataStore
  (AC 8). Verify by: `ChainRepositoryImpl` (`userPreferences.selectedNetwork` + `ChainMapper`).
- [ ] **C3** `setSelectedChain()` persists to DataStore; unsupported ID handled (AC 9–10) —
  judge the `DataResult.Error` vs `IllegalArgumentException` deviation explicitly.
- [ ] **C4** `getChainById()` returns null for unsupported IDs (AC 11).
- [ ] **C5** `getSupportedChains()` returns a Flow of all 4 chains (AC 12).
- [ ] **C6** Three use cases exist with `@Inject constructor` (AC 13). Verify by:
  `usecase/chain/` 3 files.
- [ ] **C7** `ChainSelectorDropdown`, `ChainBadge`, `ChainUi`, `ChainIconMapper`,
  `Chain.toChainUi()` all exist (AC 14–18). Verify by: read each.
- [ ] **C8** 4 chain vector drawables exist in `core-ui` resources (AC 19) — verified present;
  check `ChainIconMapper` maps all 4 + unknown fallback.
- [ ] **C9** `ChainRepository` Hilt binding in `data` (AC 20). Verify by: `RepositoryModule.kt`.
- [ ] **C10** Chain-config single-source judgment (AC 21) — domain `rpcUrl` vs
  `ChainConfigProvider`; record verdict + evidence, propose disposition (e.g. Phase 4 item or
  accept the split with a note).
- [ ] **C11** Domain stays Android-free (AC 24) — `Chain` icon reference is an `Int` resource id,
  not a drawable type. Verify by: `grep -rn 'android\\.' domain/src/main`.
- [ ] **C12** `data` does not depend on `core-ui` (AC 25). Verify by: `data/build.gradle.kts`.
- [ ] **C13** KDoc on public classes; no `TODO()` (AC 22–23; E7 = 0).
- [ ] **C14** **0 tests** — record as finding; owning item 2.0.6 or new test item.
- [ ] **C15** `setChainVisible`/`getVisibleChains` (extra surface beyond the prompt) — check for
  orphan callers and consistent semantics. Verify by: `ChainRepositoryImpl.kt` + usage grep.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
