# CR 2.3 — Chain management — repository, use cases, selector UI

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **1 finding recorded 2026-09-24 — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.3 Chain management (Phase 0) |
| Prompt | `doc/prompts/13-Task-2.3-Chain-Management-Repository-Use-Cases-Chain-Selector-UI.md` |
| Landing modules | `domain` (Chain model, `ChainRepository`, 3 use cases) · `data` (`ChainRepositoryImpl`, `ChainMapper`) · `core:core-ui` (`ChainSelectorDropdown`, `ChainBadge`, `ChainUi`, `ChainUiMapper`, `ChainIconMapper`, 4 drawables) |
| Tests | 4 chain-related test files (ChainTest, SetSelectedChainUseCaseTest, ChainRepositoryImplTest, ChainMapperTest) |
| `doc/08` anchor | AC-2.1 (multi-chain) |

## Measured file inventory (2026-09-21)

- `domain/model/chain/Chain.kt` (incl. `SupportedChains`), `domain/repository/ChainRepository.kt`,
  `usecase/chain/`: `GetSupportedChainsUseCase`, `GetSelectedChainUseCase`, `SetSelectedChainUseCase`.
- `data/repository/ChainRepositoryImpl.kt`, `data/mapper/ChainMapper.kt`.
- `core/core-ui`: `components/ChainBadge`, `components/ChainSelectorDropdown`, `components/ChainUi`,
  `mapper/ChainUiMapper`, `util/ChainIconMapper`; drawables `ic_chain_ethereum`,
  `ic_chain_sepolia`, `ic_chain_bsc`, `ic_chain_polygon` (verified present 2026-09-21).

## Known notes (pre-registered findings — do not re-create)

- The roadmap's "2.3 — no tests" claim is **refuted by current evidence**: 4 chain-related test files
  exist (the 1.4/1.5 fix rounds added `ChainMapperTest` and updated `ChainRepositoryImplTest`).
  Remaining gap: core-ui composables have no UI tests (TC-UI → 2.0.6).
- Prompt AC 10 said `setSelectedChain` throws `IllegalArgumentException`; the implementation returns
  `DataResult.Error` — **judged correct**: tech-stack §3 requires repositories to return
  `DataResult`, never throw. The prompt text predates the convention.
- `setChainVisible`/`getVisibleChains` are extra surface beyond the prompt with **no production
  callers** (tests only) — pre-built for 3.4 settings; noted, not a defect.

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :domain:compileKotlin :data:assembleDebug :core:core-ui:assembleDebug`
  → **BUILD SUCCESSFUL** (2026-09-24).
- [x] **C2** ✓ pass — `ChainRepository` interface (6 methods) + `ChainRepositoryImpl` with the 4
  hardcoded chains (1, 11155111, 56, 137).
- [x] **C3** ✓ pass — `getSelectedChain` emits from DataStore (`selectedNetwork` → `ChainMapper`),
  fallback to Ethereum mainnet on unknown.
- [x] **C4** ✓ pass — `setSelectedChain` persists via `NetworkType` (post-1.4 round-trips all four
  chains); unsupported → `DataResult.Error` (judged correct vs the prompt's throw, see Known notes).
- [x] **C5** ✓ pass — `getChainById` returns null for unsupported IDs.
- [x] **C6** ✓ pass — `getSupportedChains` returns a Flow of all 4.
- [x] **C7** ✓ pass — 3 use cases with `@Inject` + `operator fun invoke`.
- [x] **C8** ✓ pass — `ChainSelectorDropdown` (selected chain + `onChainSelected: (Int) -> Unit`),
  `ChainBadge`, `ChainUi`, `ChainIconMapper`, `Chain.toChainUi()` all exist.
- [x] **C9** ✓ pass — 4 chain vector drawables present; `ChainIconMapper` maps all 4 + a fallback to
  the Ethereum icon.
- [x] **C10** ✗ finding 2.3-1 — the chain-config single-source judgment: `Chain.rpcUrl` and
  `Chain.iconResName` are **never read by any code** — `rpcUrl` is empty for Ethereum/Sepolia
  (real URLs live in `ChainConfigProvider`) and `iconResName` values don't match the drawable
  names (`ChainIconMapper` uses its own chainId map). Dead, misleading fields.
- [x] **C11** ✓ pass — `ChainRepository` Hilt binding in `data` (`RepositoryModule`).
- [x] **C12** ✓ pass — domain Android-free (0 `android.` imports; icon reference is a `String`).
- [x] **C13** ✓ pass — `data` does not depend on `core-ui`.
- [x] **C14** ✓ pass — KDoc on the public classes; `grep 'TODO()'` → **0**.
- [x] **C15** ✓ pass (noted) — `setChainVisible`/`getVisibleChains` orphan surface pre-built for 3.4.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2.3-1 | **Medium** | `domain/model/chain/Chain.kt:13,17` | `Chain.rpcUrl` and `Chain.iconResName` are dead, misleading fields: **no code reads them** (grep: the only `.rpcUrl` read is `ChainNetworkConfig.rpcUrl` in `Web3jProvider`); `rpcUrl` is `""` for Ethereum/Sepolia while the real URLs live in `ChainConfigProvider`; `iconResName` (`ic_ethereum`/`ic_bnb`/`ic_polygon`) matches no drawable (`ChainIconMapper` maps chainId → `ic_chain_*` directly). Fix: remove both fields from the domain model (single sources: `ChainConfigProvider` for RPC, `ChainIconMapper` for icons) — or wire them properly if a future consumer is planned. | reads 2026-09-24: `grep -rnE '\.rpcUrl|iconResName'` → no readers outside the model; `Chain.kt:31,43` empty URLs | developer fix round (this CR) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
