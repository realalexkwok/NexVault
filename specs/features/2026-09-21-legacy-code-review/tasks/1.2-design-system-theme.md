# CR 1.2 — Design system & theme

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-21 — sign-off recorded** (open findings 1.2-5 / 1.2-6 / 1.2-7 stay in
> the table as the transfer channel for their owning items) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.2 Design system & theme (Phase 0) |
| Prompt | `doc/prompts/02-Task-1.2-Design-System-Theme.md` |
| Landing module | `core:core-ui` (29 source files + `res/font/` 4 Inter TTFs + `res/values/strings.xml`) |
| Tests | **0** (unchanged — 1.2-5, owned by 2.0.6) |
| `doc/08` anchor | AC-1.2 |

## Measured file inventory (2026-09-21)

- Prompt-02 originals: `theme/` 6 (`Color`, `Type`, `Shape`, `Dimens`, `NexVaultTheme`,
  `NexVaultColors`), `components/` (`GradientBackground`, `NexVaultBottomSheet`, `NexVaultButton`,
  `NexVaultCard`, `NexVaultTextField`, `NexVaultTopBar`, `PinInputField`, `ShimmerPlaceholder`,
  `EmptyStateView`), `animation/` 3 (`CountUpAnimation`, `NexVaultAnimations`, `PulseAnimation`),
  `preview/ThemePreviewWrapper`.
- Arrived with later items (reviewed under those items, listed here for completeness):
  `ChainBadge`, `ChainSelectorDropdown`, `ChainUi` (2.3); `ChainUiMapper`, `ChainIconMapper` (2.3);
  `TokenIcon`, `SimpleLineChart`, `PriceChangeChip`, `TransactionRow` (2.4/2.5); `FormatUtils` (2.5).
- Post-fix additions (developer round, verified): `res/font/inter_{regular,medium,semibold,bold}.ttf`,
  `core-ui` + per-feature `strings.xml`, 17 `@Preview` functions.
- Measure command: `find core/core-ui/src -name '*.kt' | wc -l` → 29.

## Known notes (pre-registered findings — do not re-create)

- Dark theme is the default and currently the only theme **in practice**: the app hardcodes
  `NexVaultTheme(darkTheme = true)` at all three call sites (`MainActivity.kt:45`,
  `MainScreen.kt:240,248`) — consistent with `mission.md`.
- `core-ui` depends on `domain` (allowed, tech-stack §2.5) plus approved libs (coil, lottie, vico);
  `kotlinx-collections-immutable` is declared-but-unused — pre-registered **4.5**.
- 0 tests — `doc/07` TC-UI (10 cases) has no home; traceability is owned by **2.0.6** (finding 1.2-5).
- detekt: `core-ui` weighted issues reduced by the fix round; gates owned by **2.0.5**.

## Review checklist

- [x] **C1** ✓ pass — Material 3 with a dark default in practice: `NexVaultTheme(darkTheme=true)` at
  all three call sites (`MainActivity.kt:45`, `MainScreen.kt:240/248`).
- [x] **C2** ✓ pass — Full dark+light palettes (Color.kt), `NexVaultColors` crypto tokens (incl. new
  `info` token added by the fix round), `NexVaultTheme.colors`/`dimens` accessors.
- [x] **C3** ✓ pass — Complete typography scale in `Type.kt`.
- [x] **C4** ✓ pass — `Shape.kt` + `Dimens.kt` token sets (12 new tokens added by the fix round).
- [x] **C5** ✓ pass (post-fix) — Feature modules now carry **0** hardcoded `Text("…")`, **0** hardcoded
  `.dp`/`.sp` literals, **0** `Color(0x…)`, **0** literal `contentDescription`s (fresh greps
  2026-09-21); per-feature `strings.xml` exist (tokens 16, onboarding 48, home 24, auth 11 + 1
  plurals). Residual interpolated strings → finding 1.2-6.
- [x] **C6** ✓ pass (post-fix) — `@Preview` in core-ui: **0 → 17**; every component file is covered
  (`ChainUi.kt` is a data class, not a composable). Strict per-composable previews are a 4.17 nicety,
  noted in `validation.md`.
- [x] **C7** ✓ pass — Light theme fully coherent (palette + scheme + `getLightNexVaultColors()`).
- [x] **C8** ✓ pass (noted) — 3 animation utilities exist as specified; still unreferenced by screens.
- [x] **C9** ✓ pass — `core-ui` deps: domain + approved libs; unused `kotlinx-collections-immutable`
  pre-registered **4.5**.
- [x] **C10** ✓ pass — No duplicated shared composables in features.
- [x] **C11** ✗ finding 1.2-5 — **0 tests** in `core-ui` (unchanged; owned by 2.0.6).
- [x] **C12** ✓ pass (post-fix) — Components use theme tokens; `TransactionRow` now uses
  `NexVaultTheme.colors.info`/`.positive` (was hardcoded — 1.2-3, verified fixed).

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).
> Findings 1.2-1 … 1.2-4 were **fixed by the developer and verified by the reviewer on
2026-09-21 — rows deleted**; evidence stays in `../validation.md` §1.2 (V1–V10).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1.2-5 | **Medium** | `core/core-ui` | **0 tests** — no unit or Compose UI tests for theme tokens, components, or utilities. `doc/07` TC-UI (10 cases) has no implementing tests. Untouched by the fix round (per handoff). | `find core/core-ui/src/test -name '*.kt'` → none (2026-09-21) | **2.0.6** (TC traceability) | **Open** |
| 1.2-6 | **Low** | `ImportWalletScreen.kt:276,342` · `UnlockScreen.kt:255` | Three interpolated UI strings remain hardcoded: `"$wordCount / 12 words"`, `"${cleaned.length} / 64 characters"` (ImportWalletScreen), `"${uiState.lockoutRemainingSeconds}s"` (UnlockScreen) — not resource-backed (plurals/placeholders needed). The "N monogram" candidate is **refuted**: `TokenIcon.kt:47` renders `symbol.firstOrNull()?.uppercase() ?: "?"` — data-driven, the `"?"` is a fallback glyph, not a translatable string. | fresh reads 2026-09-21 (lines verified) | **4.17** | **Open** |
| 1.2-7 | **Low** | `SetPinViewModel.kt:189–193` | `set_pin_failed_with_reason` ("Failed to set PIN: %1$s") is always used with `e.message.orEmpty()` — a null/empty exception message renders a **dangling "Failed to set PIN: "** with a trailing colon. The claimed "Verification error: " counterpart is **not reproduced**: no such string exists; `VerifyMnemonicViewModel.kt:80–88` falls back to `verify_mnemonic_load_failed` ("Failed to load mnemonic") cleanly. | fresh reads 2026-09-21 (lines verified) | **4.17** | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Sign-off recorded in `../validation.md` — 2026-09-21 (user-confirmed close per Q9; verification V1–V12).
