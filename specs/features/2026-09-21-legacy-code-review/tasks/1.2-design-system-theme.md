# CR 1.2 — Design system & theme

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.2 Design system & theme (Phase 0) |
| Prompt | `doc/prompts/02-Task-1.2-Design-System-Theme.md` |
| Landing module | `core:core-ui` (29 source files) |
| Tests | **0** |
| `doc/08` anchor | AC-1.2 |

## Measured file inventory (2026-09-21)

- Prompt-02 originals: `theme/` 6 (`Color`, `Type`, `Shape`, `Dimens`, `NexVaultTheme`,
  `NexVaultColors`), `components/` (`GradientBackground`, `NexVaultBottomSheet`, `NexVaultButton`,
  `NexVaultCard`, `NexVaultTextField`, `NexVaultTopBar`, `PinInputField`, `ShimmerPlaceholder`,
  `EmptyStateView`), `animation/` 3 (`CountUpAnimation`, `NexVaultAnimations`, `PulseAnimation`),
  `preview/ThemePreviewWrapper`.
- Arrived with later items (reviewed under those items, listed here for completeness):
  `ChainBadge`, `ChainSelectorDropdown`, `ChainUi` (2.3); `ChainUiMapper`, `ChainIconMapper` (2.3);
  `TokenIcon`, `SimpleLineChart`, `PriceChangeChip`, `TransactionRow` (2.4/2.5); `FormatUtils` (2.5).
- Measure command: `find core/core-ui/src -name '*.kt' | wc -l` → 29.

## Known notes

- Dark theme is the default and currently the only theme (`mission.md`; `NexVaultTheme(darkTheme = true)`).
- `core-ui` depends on `domain` (allowed, tech-stack §2.5); it must not depend on `data`/`app`/features.
- 0 tests — `doc/07` TC-UI (10 cases) has no home; traceability is owned by **2.0.6**.

## Review checklist

- [ ] **C1** Material 3, dark default. Verify by: `theme/NexVaultTheme.kt`.
- [ ] **C2** Purple/indigo color scheme via tokens (AC-1.2; prompt AC 4–5): custom accessors
  (`NexVaultTheme.colors.positive` etc.) compile and work. Verify by: `theme/Color.kt` + `NexVaultColors.kt`.
- [ ] **C3** Typography scale defined; no hardcoded text styles in components (AC-1.2).
  Verify by: `theme/Type.kt` + spot-check `components/`.
- [ ] **C4** Shape + dimensions from tokens. Verify by: `theme/Shape.kt`, `theme/Dimens.kt`.
- [ ] **C5** No hardcoded colors/dimensions/strings in **feature** modules (AC-1.2).
  Verify by: `grep -rnE 'Color\(0x|\\.sp|\\.dp' feature --include='*.kt'`.
- [ ] **C6** All shared composables have `@Preview` (AC-1.2). Verify by: `grep -c '@Preview' core/core-ui/src/main`
  vs composable count.
- [ ] **C7** Light-theme coherence (prompt AC 7): `darkTheme = false` has a real light palette, not
  missing colors. Verify by: `theme/Color.kt` light values + `NexVaultTheme` branches.
- [ ] **C8** Animation utilities exist and are referenced or clearly library-ready (prompt item 7).
  Verify by: `animation/` 3 files + usage grep.
- [ ] **C9** Dependency edge clean. Verify by: `core/core-ui/build.gradle.kts` (domain only).
- [ ] **C10** No duplicated components across features after 2.4/2.5 moved `TokenIcon`/`SimpleLineChart`/
  `FormatUtils` into `core-ui`. Verify by: `grep -rln 'fun TokenIcon\|fun SimpleLineChart\|fun formatFiatValue' feature`.
- [ ] **C11** 0 tests — record as finding (Medium) unless 2.0.6/TC-UI ownership covers it; decide at scan.
- [ ] **C12** Components use `NexVaultTheme` tokens exclusively (prompt AC 6). Verify by: spot-read
  each prompt-02 component for hardcoded MaterialTheme overrides.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
