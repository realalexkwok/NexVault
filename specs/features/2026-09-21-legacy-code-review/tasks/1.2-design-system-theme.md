# CR 1.2 — Design system & theme

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **5 findings recorded 2026-09-21 — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.2 Design system & theme (Phase 0) |
| Prompt | `doc/prompts/02-Task-1.2-Design-System-Theme.md` |
| Landing module | `core:core-ui` (29 source files) |
| Tests | **0** |
| `doc/08` anchor | AC-1.2 |

## Measured file inventory (2026-09-21)

- Prompt-02 originals: `theme/` 6 (`Color`, `Type`, `Shape`, `Dimens`, `NexVaultTheme`,
  `NexVaultColors`), `components/` (`GradientBackground`, `NexVaultBottomSheet`, `NexVaultButton`,
  `NexVaultCard`, `NexVaultTextField`, `NexVaultTopBar`, `PinInputField`, `ShimmerPlaceholder`,
  `EmptyStateView`), `animation/` 3 (`CountUpAnimation`, `NexVaultAnimations`, `PulseAnimation`),
  `preview/ThemePreviewWrapper`.
- Arrived with later items (reviewed under those items, listed here for completeness):
  `ChainBadge`, `ChainSelectorDropdown`, `ChainUi` (2.3); `ChainUiMapper`, `ChainIconMapper` (2.3);
  `TokenIcon`, `SimpleLineChart`, `PriceChangeChip`, `TransactionRow` (2.4/2.5); `FormatUtils` (2.5).
- Measure command: `find core/core-ui/src -name '*.kt' | wc -l` → 29.

## Known notes (pre-registered findings — do not re-create)

- Dark theme is the default and currently the only theme **in practice**: the app hardcodes
  `NexVaultTheme(darkTheme = true)` at all three call sites (`MainActivity.kt:45`,
  `MainScreen.kt:240,248`) — consistent with `mission.md`.
- `core-ui` depends on `domain` (allowed, tech-stack §2.5) plus approved libs (coil, lottie, vico);
  `kotlinx-collections-immutable` is declared-but-unused — pre-registered **4.5**.
- 0 tests — `doc/07` TC-UI (10 cases) has no home; traceability is owned by **2.0.6**.
- detekt: `core-ui` 5 weighted issues — owned by **2.0.5**.

## Review checklist

- [x] **C1** ✓ pass — Material 3 with a dark default in practice: `NexVaultTheme(darkTheme=true)` at
  all three call sites (`MainActivity.kt:45`, `MainScreen.kt:240/248`); the composable's parameter
  default (`isSystemInDarkTheme()`) is never exercised because every caller forces dark. Consistent
  with `mission.md` ("dark theme is the default and currently the only theme").
- [x] **C2** ✓ pass — Full token sets in `Color.kt` (dark + light palettes, all M3 slots),
  crypto-specific `NexVaultColors` (positive/negative/warning/cardSurface/cardBorder/shimmer/
  gradient/text emphasis, dark + light variants) and the `NexVaultTheme.colors` /
  `NexVaultTheme.dimens` accessors (prompt AC 5). Scheme: indigo-blue primary `0xFF2D5AF0` +
  purple tertiary `0xFF7B61FF` + teal secondary — AC-1.2's "purple/indigo" substantially met.
- [x] **C3** ✓ pass (with font note → finding 1.2-4) — Complete typography scale in `Type.kt`;
  but the file admits "Using default system font as fallback, Inter can be added" — AC-1.2's
  "Inter / Space Grotesk" fonts are **not** adopted.
- [x] **C4** ✓ pass — `Shape.kt` (5 corner radii) and `Dimens.kt` (spacing, icon sizes,
  button/input heights, nav heights, radii) token sets defined.
- [x] **C5** ✗ finding 1.2-1 — Feature modules contain hardcoded values despite the token sets:
  1 hardcoded color (`TokenDetailScreen.kt:333` `Color(0xFF4CAF50)`), **149** hardcoded
  `.dp`/`.sp` literals, and ≥10 hardcoded user-facing strings with **no string resources** under
  any `feature/*/res/values` — violating AC-1.2 ("no hardcoded colors, dimensions, or strings
  anywhere in feature modules").
- [x] **C6** ✗ finding 1.2-2 — **0** `@Preview` annotations in all 29 core-ui files
  (`grep -rn '@Preview' core/core-ui` → 0), violating AC-1.2 ("all shared composables have
  Compose Preview functions"). Only app-level previews exist (`MainScreen.kt:240,248`).
- [x] **C7** ✓ pass — Light theme is fully coherent: complete light palette + light color scheme +
  `getLightNexVaultColors()`; `NexVaultTheme(darkTheme=false)` would render a real light theme
  (prompt AC 7).
- [x] **C8** ✓ pass (noted) — The 3 animation utilities (`CountUpAnimation`, `PulseAnimation`,
  `NexVaultAnimations`) exist as specified by prompt item 7; they are currently **unreferenced**
  by any screen (grep shows no usage outside `animation/`) — library-ready, not a defect.
- [x] **C9** ✓ pass — `core/core-ui/build.gradle.kts`: `domain` + hilt + Compose BOM + coil +
  lottie + vico + kotlinx-collections-immutable (all approved, tech-stack §4). The unused
  `kotlinx-collections-immutable` is pre-registered **4.5**.
- [x] **C10** ✓ pass — No duplicated shared composables in features:
  `grep -rln 'fun TokenIcon|fun SimpleLineChart|fun formatFiatValue|fun truncateAddress' feature` → empty.
- [x] **C11** ✗ finding 1.2-5 — **0 tests** in `core-ui`; `doc/07` TC-UI (10 cases) has no home.
  Traceability is 2.0.6's; the component-level coverage gap itself is recorded here.
- [x] **C12** ✓ pass with one exception (finding 1.2-3) — Spot-read components use theme tokens:
  `NexVaultButton` uses `MaterialTheme.colorScheme` + `NexVaultDimens` exclusively. The only
  component-file hardcoded colors are `TransactionRow.kt:62,64`
  (`Color(0xFF2196F3)` swap, `Color(0xFF4CAF50)` received) instead of `NexVaultTheme.colors.*`
  — violates prompt AC 6 ("components use theme tokens exclusively").

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1.2-1 | **Medium** | `feature/*` (all four implemented feature modules) | Feature modules bypass the design tokens: **149** hardcoded `.dp`/`.sp` literals, 1 hardcoded color (`TokenDetailScreen.kt:333` `Color(0xFF4CAF50)`), and ≥10 hardcoded user-facing strings with **no string resources** under `feature/*/res/values`. Violates AC-1.2 "No hardcoded colors, dimensions, or strings anywhere in feature modules"; `NexVaultDimens`/`NexVaultColors` exist but features don't use them. | 2026-09-21 greps: dims `grep -rnE '[0-9]+\.(dp|sp)' feature` → 149; colors `grep -rnE 'Color\(0x' feature app` → 1; strings `grep -rnE 'Text\("[A-Za-z]'` → 10 hits (HomeScreen, AddTokenDialog, TokenDetailScreen, ImportWalletScreen) | **4.17** (owner-approved 2026-09-21) | **Open** |
| 1.2-2 | **Low** | `core/core-ui` (all 29 files) | Zero `@Preview` annotations in core-ui — AC-1.2 "All shared composables have Compose Preview functions" unmet. Only app-level previews exist (`MainScreen.kt:240,248`). | `grep -rn '@Preview' core/core-ui` → **0** (2026-09-21) | **4.17** | **Open** |
| 1.2-3 | **Low** | `core/core-ui/src/main/java/com/nexvault/wallet/core/ui/components/TransactionRow.kt:62,64` | Component hardcodes `Color(0xFF2196F3)` (swap) and `Color(0xFF4CAF50)` (received) instead of `NexVaultTheme.colors.positive`/`warning` — violates prompt 02 AC 6 ("components use theme tokens exclusively"). File arrived with 2.4/2.5; the convention applies to all core-ui. | grep `Color(0x` in components → only these 2 hits (2026-09-21) | **4.17** | **Open** |
| 1.2-4 | **Low** | `core/core-ui/src/main/java/com/nexvault/wallet/core/ui/theme/Type.kt:13` | Typography falls back to `FontFamily.Default` — AC-1.2's "Inter / Space Grotesk" fonts are not bundled or loaded (in-code comment admits the fallback). | `Type.kt` comment + `NexVaultFontFamily = FontFamily.Default` (2026-09-21) | **4.17** (or 3.7 polish) | **Open** |
| 1.2-5 | **Medium** | `core/core-ui` | **0 tests** — no unit or Compose UI tests for theme tokens, components, or the chart/animation utilities. `doc/07` TC-UI (10 cases) has no implementing tests. | `find core/core-ui/src/test -name '*.kt'` → none (2026-09-21); tech-stack §5 table "core-ui 0 tests" | **2.0.6** (TC traceability) + note: component-level test suite needs a home | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
