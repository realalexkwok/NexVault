# CR 1.5 — Domain models & repository interfaces

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **2 findings recorded 2026-09-22 — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.5 Domain models & repository interfaces (Phase 0) |
| Prompt | `doc/prompts/05-Task-1.5-Domain-Models-And-Repository-Interfaces.md` |
| Landing module | `domain` (pure JVM) — 32 main + 10 test files |
| Tests | **63 pass** (fresh 2026-09-22 rerun) |
| `doc/08` anchor | AC-1.3 (wallet creation), AC-1.4 (import), Phase 2 checklist |

## Measured file inventory (2026-09-21)

- Models: `model/auth/AuthModels`, `model/chain/Chain`, `model/common/DataResult`,
  `model/common/Exceptions`, `model/token/Token`, `model/transaction/Transaction`, `model/wallet/Wallet`.
- Repositories: `AuthRepository`, `ChainRepository`, `TokenRepository`,
  `TransactionRepository`, `WalletRepository`.
- Use cases (20): `auth` 5, `chain` 3, `token` 8, `wallet` 4.
- Tests: `ChainTest`, `DataResultTest`, `ExceptionsTest`, `TokenTest`,
  `AddCustomTokenUseCaseTest`, `ChangePinUseCaseTest`, `ImportWalletUseCaseTest`,
  `SetPinUseCaseTest`, `SetSelectedChainUseCaseTest`, `VerifyPinUseCaseTest`.
- Measure command: `find domain/src -name '*.kt' | wc -l` → 42.

## Known notes (pre-registered findings — do not re-create)

- `DataResult` has **no `Loading` variant** (drift correction, tech-stack §8) — code is right,
  archive was wrong.
- `TransactionRepository` declares `estimateGas` + 3 write paths that `TransactionRepositoryImpl`
  leaves unimplemented — **2.6** entry condition (roadmap).
- **2.3 evidence handed over:** `Chain.rpcUrl` is populated only for BSC/Polygon and **empty for
  Ethereum/Sepolia** (the real RPC URLs, incl. API-key templating, live in `core-network`
  `ChainConfigProvider`); `iconResName` values (`ic_ethereum`/`ic_bnb`/`ic_polygon`) do not
  match the drawable file names (`ic_chain_ethereum`/`ic_chain_bsc`/`ic_chain_polygon`) — reviewed
  under 2.3 C10.

## Review checklist

- [x] **C1** ✓ pass — Pure JVM: `grep -rn 'android\\.' domain/src/main` → **0**.
- [x] **C2** ✓ pass — `./gradlew :domain:test --rerun-tasks` → **BUILD SUCCESSFUL, 63 tests, 0
  failures, 0 skipped** (2026-09-22).
- [x] **C3** ✓ pass — All 5 repository interfaces complete with KDoc (method surfaces read;
  `/**` counts 7–13 per file).
- [x] **C4** ✗ finding 1.5-1 — 19 of 20 use cases use `operator fun invoke`; **`ImportWalletUseCase`**
  instead exposes two named methods (`fromMnemonic`, `fromPrivateKey`) — two actions in one class,
  breaking the one-class-per-action convention (tech-stack §3).
- [x] **C5** ✓ pass — `DataResult` helpers `map`/`flatMap`/`getOrNull`/`getOrThrow` +
  `Result<T>.toDataResult()` all present; exactly two variants, no `Loading`.
- [x] **C6** ✓ pass — All 10 domain exceptions extend `NexVaultException`.
- [x] **C7** ✓ pass — `SupportedChains` holds exactly the 4 chains with correct identity data
  (chainIds 1/11155111/56/137, names, symbols, explorer URLs, testnet flags, CoinGecko ids). The
  `rpcUrl` field's incomplete population is handed to 2.3 (see Known notes).
- [x] **C8** ✓ pass (noted) — `ImportWalletUseCase` validates format **before** the repository:
  mnemonic word count 12/24, private key 64-hex after `0x` stripping. Note: full BIP-39 word-list
  validation happens inside the data layer (`MnemonicManager.validateMnemonic`), not here — the
  pre-repository check is format-level, which matches the prompt's intent.
- [x] **C9** n/a → pre-registered **2.6** — write-path declarations recorded for the 2.6
  entry-condition check.
- [x] **C10** ✗ finding 1.5-2 — `TokenRepository.seedDefaultTokens` exists and is called from the
  **wallet-creation/import** paths (`WalletRepositoryImpl`:93/154/210 → :228) but **not on chain
  switch**, while roadmap 2.9 ("Default token list") is marked `[ ]` — the roadmap understates the
  implemented half and the chain-switch half is missing.
- [x] **C11** ✓ noted — `Chain.rpcUrl` presence + empty values and the `iconResName` mismatch
  recorded as evidence; the consolidation judgment belongs to **2.3 C10**.
- [x] **C12** ✓ pass — `domain/build.gradle.kts` is JVM-only (kotlin-jvm + coroutines +
  `javax.inject`).
- [x] **C13** ✓ pass — 16 `data class`es, **0** `var` fields — immutable models.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1.5-1 | **Low** | `domain/src/main/java/com/nexvault/wallet/domain/usecase/wallet/ImportWalletUseCase.kt` | The use case deviates from the binding convention "one class per action, `operator fun invoke(...)`, constructor-injected" (tech-stack §3): it bundles **two actions** (`fromMnemonic`, `fromPrivateKey`) with no `invoke` operator. All other 19 use cases conform. Either split into `ImportFromMnemonicUseCase` + `ImportFromPrivateKeyUseCase` (following the convention) or amend the convention to allow multi-method use cases. | `grep -c 'operator fun invoke' usecase` → 19 of 20 files (2026-09-22); `@Inject` present | proposed new Phase 4 item (domain API alignment) — owner approval at sign-off | **Open** |
| 1.5-2 | **Medium** | `TokenRepository.kt:83` + `WalletRepositoryImpl.kt:226–229` vs `specs/roadmap.md` 2.9 | Roadmap 2.9 ("Default token list — seed tokens on wallet creation / chain switch") is marked `[ ]`, but the **wallet-creation half already exists** (`seedDefaultTokensForSupportedChains` on create/import/restore paths) while the **chain-switch half does not** (no call from `SetSelectedChainUseCase`/`RefreshBalancesUseCase`). 2.9 must be re-planned to reflect reality: mark the creation half implemented and scope the remaining chain-switch seeding. | grep `seedDefaultTokens` call sites → only `WalletRepositoryImpl`:93/154/210 (2026-09-22) | **2.9 re-planning** (roadmap accuracy) — owner decision at sign-off | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
