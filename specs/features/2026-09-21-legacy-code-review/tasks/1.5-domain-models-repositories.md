# CR 1.5 — Domain models & repository interfaces

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[x]` **closed 2026-09-23 — sign-off recorded** (recommended-option decisions sanctioned by the owner at
> close) — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.5 Domain models & repository interfaces (Phase 0) |
| Prompt | `doc/prompts/05-Task-1.5-Domain-Models-And-Repository-Interfaces.md` |
| Landing module | `domain` (pure JVM) — 33 main + 12 test files (post-fix) |
| Tests | **67 pass** (fresh 2026-09-23 clean rerun; repo 231 / 0 / 1) |
| `doc/08` anchor | AC-1.3 (wallet creation), AC-1.4 (import), Phase 2 checklist |

## Measured file inventory (2026-09-21)

- Models: `model/auth/AuthModels`, `model/chain/Chain`, `model/common/DataResult`,
  `model/common/Exceptions`, `model/token/Token`, `model/transaction/Transaction`, `model/wallet/Wallet`.
- Repositories: `AuthRepository`, `ChainRepository`, `TokenRepository`,
  `TransactionRepository`, `WalletRepository`.
- Use cases (21 post-fix): `auth` 5, `chain` 3, `token` 8, `wallet` 5
  (`ImportWalletUseCase` split into `ImportFromMnemonicUseCase` + `ImportFromPrivateKeyUseCase`).
- Tests: 12 files (post-fix: `ImportFromMnemonicUseCaseTest`, `ImportFromPrivateKeyUseCaseTest`,
  `RefreshBalancesUseCaseTest` added; `ImportWalletUseCaseTest` deleted).

## Known notes (pre-registered findings — do not re-create)

- `DataResult` has **no `Loading` variant** (drift correction, tech-stack §8).
- `TransactionRepository` write paths unimplemented in data — **2.6** entry condition.
- **2.3 evidence handed over:** `Chain.rpcUrl` empty for Ethereum/Sepolia; `iconResName` mismatch —
  reviewed under 2.3 C10.

## Review checklist

- [x] **C1** ✓ pass — 0 Android imports in `domain/src/main`.
- [x] **C2** ✓ pass — clean rerun: `:domain:test` **67 tests, 0 failures, 0 skipped**; repo
  **231 / 0 / 1**.
- [x] **C3** ✓ pass — 5 repository interfaces complete with KDoc.
- [x] **C4** ✓ pass (post-fix) — all **21** use cases now use `operator fun invoke` with
  `@Inject constructor` (finding 1.5-1, verified fixed: the split pair replaces the non-conforming
  class; `ImportWalletViewModel` dispatches by mode).
- [x] **C5** ✓ pass — `DataResult` helpers complete; no `Loading`.
- [x] **C6** ✓ pass — all exceptions extend `NexVaultException`.
- [x] **C7** ✓ pass — 4 chains with correct identity data; rpcUrl incompleteness handed to 2.3.
- [x] **C8** ✓ pass (noted) — format validation before the repository (word count / 64-hex), now in
  the two split use cases with identical logic.
- [x] **C9** n/a → pre-registered **2.6**.
- [x] **C10** ✓ pass (post-fix) — chain-switch seeding implemented in `RefreshBalancesUseCase`
  (idempotent, best-effort: seeding errors never fail the refresh; 4 new tests incl. switch + failure
  cases) — finding 1.5-2 verified fixed; roadmap 2.9 status corrected to `[~]`.
- [x] **C11** ✓ noted — evidence handed to **2.3 C10**.
- [x] **C12** ✓ pass — JVM-only build.
- [x] **C13** ✓ pass — 16 data classes, 0 `var`.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).
> **Both findings (1.5-1, 1.5-2) were fixed by the developer and verified by the reviewer on
2026-09-23 — rows deleted.** Evidence stays in `../validation.md` §1.5.

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none — both verified-fixed and deleted)_ | | | | | | |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [x] Sign-off recorded in `../validation.md` — 2026-09-23 (user-confirmed close per Q9; verification V1–V6).
