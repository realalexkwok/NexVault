# CR 1.5 — Domain models & repository interfaces

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.5 Domain models & repository interfaces (Phase 0) |
| Prompt | `doc/prompts/05-Task-1.5-Domain-Models-And-Repository-Interfaces.md` |
| Landing module | `domain` (pure JVM) — 32 main + 10 test files |
| Tests | **63 pass** |
| `doc/08` anchor | AC-1.3 (wallet creation), AC-1.4 (import), Phase 2 checklist |

## Measured file inventory (2026-09-21)

- Models: `model/auth/AuthModels`, `model/chain/Chain`, `model/common/DataResult`,
  `model/common/Exceptions`, `model/token/Token`, `model/transaction/Transaction`, `model/wallet/Wallet`.
- Repositories: `AuthRepository`, `ChainRepository`, `TokenRepository`,
  `TransactionRepository`, `WalletRepository`.
- Use cases (20): `auth` 5, `chain` 3, `token` 8, `wallet` 4.
- Tests: `ChainTest`, `DataResultTest`, `ExceptionsTest`, `TokenTest`,
  `AddCustomTokenUseCaseTest`, `ChangePinUseCaseTest`, `ImportWalletUseCaseTest`,
  `SetPinUseCaseTest`, `SetSelectedChainUseCaseTest`, `VerifyPinUseCaseTest`.
- Measure command: `find domain/src -name '*.kt' | wc -l` → 42.

## Known notes (pre-registered findings — do not re-create)

- `DataResult` has **no `Loading` variant** (drift correction, tech-stack §8) — code is right,
  archive was wrong.
- `TransactionRepository` declares `estimateGas` + 3 write paths that `TransactionRepositoryImpl`
  leaves unimplemented — **2.6** entry condition (roadmap).
- **Reconciliation candidate:** `TokenRepository.seedDefaultTokens(chainId)` exists
  (`TokenRepository.kt:80–83`) and is called by `WalletRepositoryImpl` (lines 93/154/210), while
  roadmap item **2.9** ("Default token list") is marked `[ ]`. The review must record exactly what
  exists so 2.9 can be re-planned (owner decision at sign-off).
- **Reconciliation candidate:** `Chain.kt` (domain) carries `rpcUrl` per chain (e.g. line 55),
  while `core-network` `ChainConfigProvider` also holds RPC URLs — duplication reviewed under 2.3 C10.

## Review checklist

- [ ] **C1** Pure JVM — zero Android framework imports (prompt AC 4; tech-stack §2.1).
  Verify by: `grep -rn 'android\\.' domain/src/main || echo clean`.
- [ ] **C2** `:domain:test` green. Verify by: E2 — 63 tests (fresh run recorded 2026-09-21 in the
  stabilization `validation.md`; note: `domain` runs under `:domain:test`, not `testDebugUnitTest`).
- [ ] **C3** All 5 repository interfaces complete with KDoc (prompt AC 5). Verify by: read each file.
- [ ] **C4** All use cases: `@Inject constructor` + `operator fun invoke` (prompt AC 6; tech-stack §3).
  Verify by: `grep -c 'operator fun invoke' domain/src/main/java/com/nexvault/wallet/domain/usecase -r` → 20.
- [ ] **C5** `DataResult` helpers `map`/`flatMap`/`getOrNull`/`getOrThrow` +
  `Result<T>.toDataResult()` correct (prompt AC 7). Verify by: `DataResult.kt` + `DataResultTest`.
- [ ] **C6** All domain exceptions extend `NexVaultException` (prompt AC 8). Verify by: `Exceptions.kt`.
- [ ] **C7** `SupportedChains` holds exactly the 4 chains with correct data (prompt AC 9; mission
  table: 1, 11155111, 56, 137). Verify by: `Chain.kt` vs `mission.md` table.
- [ ] **C8** Import validation catches invalid mnemonics/private keys **before** calling the
  repository (prompt AC 10). Verify by: `ImportWalletUseCase` + `ImportWalletUseCaseTest`.
- [ ] **C9** `TransactionRepository` write-path surface — pre-registered **2.6**; record the exact
  declared methods for the 2.6 entry-condition check.
- [ ] **C10** `seedDefaultTokens` vs roadmap 2.9 — record the reconciliation evidence (see Known
  notes); owner decides at sign-off.
- [ ] **C11** `Chain` model `rpcUrl` field vs `ChainConfigProvider` — duplication reviewed under
  2.3; here just record the field's presence.
- [ ] **C12** `domain/build.gradle.kts` is JVM-only (no Android plugin; kotlin stdlib + coroutines +
  `javax.inject`). Verify by: read the file.
- [ ] **C13** Models immutable data classes with sensible defaults where needed. Verify by: spot-read
  `Token`, `Transaction`, `Wallet`, `AuthModels`.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
