# CR 2.2 — Database module — Room entities, DAOs, database

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **1 finding recorded 2026-09-24 — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.2 Database module (Phase 0) |
| Prompt | `doc/prompts/12-Task-2.2-Database-Module-Room-Entities-DAOs-Database.md` |
| Landing module | `core:core-database` — 4 entities + 4 DAOs + DB + DI + schema `1.json` |
| Tests | **0** |
| `doc/08` anchor | AC-2.2 (balances cache), AC-2.8 (history), 3.2/3.4 storage (Nft, AddressBook) |

## Measured file inventory (2026-09-21)

- Entities: `AddressBookEntity`, `NftEntity`, `TokenEntity`, `TransactionEntity`.
- DAOs: `AddressBookDao`, `NftDao`, `TokenDao`, `TransactionDao`.
- `NexVaultDatabase`, `di/DatabaseModule`,
  schema `core/core-database/schemas/com.nexvault.wallet.core.database.NexVaultDatabase/1.json`.
- Measure command: `find core/core-database/src -name '*.kt' | wc -l` → 10.

## Known notes (pre-registered findings — do not re-create)

- **Plaintext Room** — SQLCipher not adopted; encryption decision owned by **4.4**.
- `NftEntity`/`NftDao` and `AddressBookEntity`/`AddressBookDao` are storage for 3.2/3.4.
- `fallbackToDestructiveMigration(dropAllTables = true)` is the prompt AC 18 requirement — acceptable
  for development; re-review before any release path (3.7).
- **No tests** — `doc/07` TC-DB (3 cases) unimplemented; traceability owned by **2.0.6**.

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :core:core-database:assembleDebug` → **BUILD SUCCESSFUL**
  (2026-09-24; Room KSP codegen exercised — the `NexVaultDatabase_Impl` exists under
  `build/generated/ksp`).
- [x] **C2** ✓ pass — Composite PKs: `TokenEntity` [contractAddress, chainId]; `TransactionEntity`
  [txHash, chainId] with **4 indices** (chainId+fromAddress, chainId+toAddress, timestamp,
  chainId+status); `NftEntity` [contractAddress, tokenId, chainId]; `AddressBookEntity`
  auto-generated `Int` PK.
- [x] **C3** ✓ pass — `TokenDao`: `getTokensByChain` `ORDER BY COALESCE(fiatValue, 0) DESC` +
  `@Upsert` (`upsertTokens`).
- [x] **C4** ✓ pass — `TransactionDao`: `LIMIT :limit OFFSET :offset` pagination,
  `getAllPendingTransactions` across chains, `updateTransactionStatus` area present.
- [x] **C5** ✓ pass — `NftDao.getNftsByChain` Flow queries + upsert/delete/count.
- [x] **C6** ✓ pass — `AddressBookDao`: `LIKE '%' || :query || '%'` search over name+address and
  `addressExists`.
- [x] **C7** ✓ pass — `NexVaultDatabase` lists 4 entities, `version = 1`, `exportSchema = true`;
  schema `1.json` present (verified 2026-09-21).
- [x] **C8** ✓ pass — `DatabaseModule` `@Provides` `@Singleton` for the database + all 4 DAOs.
- [x] **C9** ✓ pass (noted) — `fallbackToDestructiveMigration(dropAllTables = true)` set (prompt AC
  18); destructive-migration-on-prod flagged for the 3.7 release re-review.
- [x] **C10** ✓ pass — `grep 'allowMainThreadQueries'` → **0**.
- [x] **C11** ✓ pass — `core-database` depends on nothing project-internal (Room/AndroidX only;
  verified in the 1.1 scan) — AC 22 satisfied.
- [x] **C12** ✓ pass — KDoc present; `grep 'TODO()'` → **0**.
- [x] **C13** ✗ finding 2.2-1 — **0 tests** in `core-database`; `doc/07` TC-DB (3 cases)
  unimplemented.
- [x] **C14** ✓ pass (spot) — `TransactionEntity` columns align with the domain
  `Transaction` model (String values for precision, nullable gas fields, status Int, type String);
  conversion handled by the 1.6 mappers.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2.2-1 | **Medium** | `core/core-database` | **0 tests** — no DAO/entity/database tests (in-memory Room is cheap and `room-testing` is already declared). `doc/07` TC-DB (3 cases) has no implementing tests. | `find core/core-database/src/test -name '*.kt'` → none (2026-09-24); tech-stack §5 "core-database 0 tests" | **2.0.6** (TC traceability) | **Open** |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
