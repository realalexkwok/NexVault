# CR 2.2 — Database module — Room entities, DAOs, database

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 2.2 Database module (Phase 0) |
| Prompt | `doc/prompts/12-Task-2.2-Database-Module-Room-Entities-DAOs-Database.md` |
| Landing module | `core:core-database` — 4 entities + 4 DAOs + DB + DI + schema `1.json` |
| Tests | **0** |
| `doc/08` anchor | AC-2.2 (balances cache), AC-2.8 (history), 3.2/3.4 storage (Nft, AddressBook) |

## Measured file inventory (2026-09-21)

- Entities: `AddressBookEntity`, `NftEntity`, `TokenEntity`, `TransactionEntity`.
- DAOs: `AddressBookDao`, `NftDao`, `TokenDao`, `TransactionDao`.
- `NexVaultDatabase`, `di/DatabaseModule`,
  schema `core/core-database/schemas/com.nexvault.wallet.core.database.NexVaultDatabase/1.json`.
- Measure command: `find core/core-database/src -name '*.kt' | wc -l` → 10.

## Known notes (pre-registered findings — do not re-create)

- **Plaintext Room** — SQLCipher not adopted; encryption decision owned by **4.4**.
- `NftEntity`/`NftDao` and `AddressBookEntity`/`AddressBookDao` are storage for 3.2/3.4.
- `fallbackToDestructiveMigration()` is a prompt requirement (AC 18) — acceptable for development;
  flag for re-review before any release path (3.7).
- **No tests** — `doc/07` TC-DB (3 cases) unimplemented; traceability owned by **2.0.6**.

## Review checklist

- [ ] **C1** Module compiles; Room KSP generates the implementation. Verify by:
  `core/core-database/build/generated/ksp/.../NexVaultDatabase_Impl` present after build.
- [ ] **C2** Composite primary keys: `TokenEntity` [contractAddress, chainId]; `TransactionEntity`
  [txHash, chainId] + **4 indices**; `NftEntity` [contractAddress, tokenId, chainId];
  `AddressBookEntity` auto `Int` PK (AC 4–7). Verify by: read each entity.
- [ ] **C3** `TokenDao.getTokensByChain()` → `Flow<List<TokenEntity>>` ordered by fiatValue DESC;
  `upsertTokens()` inserts new + updates existing (AC 8–9). Verify by: `TokenDao.kt`.
- [ ] **C4** `TransactionDao`: paginated `getTransactions()` (limit/offset),
  `updateTransactionStatus()`, `getAllPendingTransactions()` (AC 10–12).
  Verify by: `TransactionDao.kt`.
- [ ] **C5** `NftDao.getNftsByChain()` → Flow (AC 13). Verify by: `NftDao.kt`.
- [ ] **C6** `AddressBookDao.searchAddresses()` LIKE query; `addressExists()` duplicate check
  (AC 14–15). Verify by: `AddressBookDao.kt`.
- [ ] **C7** Database: 4 entities, version 1, `exportSchema = true` (AC 16). Verify by:
  `NexVaultDatabase.kt` + schema `1.json` present (verified 2026-09-21).
- [ ] **C8** `DatabaseModule`: DB as `@Singleton` + all 4 DAOs provided (AC 17).
  Verify by: `DatabaseModule.kt`.
- [ ] **C9** `fallbackToDestructiveMigration()` set (AC 18). Verify by: builder call — note the
  release-path caveat in Known notes.
- [ ] **C10** No `allowMainThreadQueries()` (AC 19). Verify by: `grep -n 'allowMainThreadQueries'` → empty.
- [ ] **C11** No dependency on `domain`, `core-network`, or any feature module (AC 22;
  tech-stack §2: `core-database` depends on nothing). Verify by: `core/core-database/build.gradle.kts`.
- [ ] **C12** KDoc on public classes; no `TODO()` (AC 20–21; E7 = 0).
- [ ] **C13** **0 tests** — record as finding; owning item 2.0.6 (TC-DB) or new test item.
- [ ] **C14** Column types match domain models (e.g. `txHash` PK length limits, decimal storage
  for amounts). Verify by: entities vs `domain/model/transaction/Transaction.kt` + mappers (1.6 C9).

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
