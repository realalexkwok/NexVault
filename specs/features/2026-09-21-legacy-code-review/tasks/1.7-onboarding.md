# CR 1.7 — Onboarding (create, import, verify, set PIN)

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.7 Onboarding (parts 1–2, Phase 0) |
| Prompts | `doc/prompts/07-Task-1.7-Part-1-...md` + `doc/prompts/08-Task-1.7-Part-2-...md` |
| Landing module | `feature:feature-onboarding` — 12 main + 5 test files |
| Tests | **30 pass** |
| `doc/08` anchor | AC-1.3 (wallet creation), AC-1.4 (import), AC-1.5 (PIN & biometric) |

## Measured file inventory (2026-09-21)

- Navigation: `OnboardingNavigation`, `OnboardingRoutes`.
- Screens: `WelcomeScreen`, `CreateWalletScreen`, `VerifyMnemonicScreen`, `ImportWalletScreen`,
  `SetPinScreen`.
- ViewModels: `WelcomeViewModel`, `CreateWalletViewModel`, `VerifyMnemonicViewModel`,
  `ImportWalletViewModel`, `SetPinViewModel`.
- Tests: one per ViewModel (5 files, 30 tests).
- Measure command: `find feature/feature-onboarding/src -name '*.kt' | wc -l` → 17.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b (BLOCKER):** `CreateWalletViewModel` calls `createWalletUseCase` from its `init`
  block, which persists the wallet **and** flips `is_wallet_set_up`; `NexVaultApp`'s
  `routingKey` (derived with `remember` + `key(...)`, `NexVaultApp.kt:56,64,67`) tears down the
  NavHost mid-flow. Result on device: wallet created, **mnemonic never shown**, no PIN set, app
  unusable. Owner: **2.0.2b** (options A/B/C pending owner decision). This review records the exact
  code paths; it does not re-own the defect.
- `FLAG_SECURE` is present on `CreateWalletScreen` and `ImportWalletScreen` (verified 2026-09-21);
  **`VerifyMnemonicScreen` shows mnemonic words** — check its FLAG_SECURE status in C6.
- Prompt 08 AC 19: back from Set PIN must go to Welcome, not to mnemonic/import screens.

## Review checklist

- [ ] **C1** Compiles + tests pass. Verify by: E2 — `feature-onboarding` 30 tests green.
- [ ] **C2** Welcome screen: app name, tagline, Create/Import buttons, navigation events
  (prompt 07 AC 4–5). Verify by: `WelcomeScreen.kt` + `WelcomeViewModel` + test.
- [ ] **C3** Create screen shows 12 mnemonic words in a 4×3 grid **on load** (07 AC 6) — the
  code-path evidence for 2.0.2b (wallet created in `init`, screen never reached). Pre-registered **2.0.2b**.
- [ ] **C4** Acknowledge checkbox enables/disables Continue (07 AC 7). Verify by: `CreateWalletScreen.kt`.
- [ ] **C5** `FLAG_SECURE` set on the window (07 AC 8). Verify by: `CreateWalletScreen.kt` +
  `ImportWalletScreen.kt` (both verified present).
- [ ] **C6** `FLAG_SECURE` on `VerifyMnemonicScreen` (mnemonic words are sensitive — doc/08
  Security). Verify by: `grep -n 'FLAG_SECURE' feature/feature-onboarding`. If absent → finding (High).
- [ ] **C7** Verify screen: shuffled words, tap-in-order, `isVerified` when correct, error + shake +
  reset on wrong selection (07 AC 9–11). Verify by: `VerifyMnemonicScreen.kt` +
  `VerifyMnemonicViewModel` + test.
- [ ] **C8** Import screen: Recovery Phrase ↔ Private Key mode toggle (08 AC 4). Verify by:
  `ImportWalletScreen.kt`.
- [ ] **C9** Real-time mnemonic validation (word count, characters) and private-key validation
  (hex, length); import button enabled only when valid (08 AC 5–7; doc/08 AC-1.4 incl. 12/24 words +
  raw key). Verify by: `ImportWalletViewModel` + test + `AddressValidator` cross-check.
- [ ] **C10** Import success → Set PIN; failure → error message (08 AC 8–9). Verify by:
  `ImportWalletViewModel` + navigation.
- [ ] **C11** Set PIN flow: title, 6 digits, confirm phase with cleared input, mismatch shake +
  reset, biometric toggle only when supported, biometric preference saved (08 AC 11–16; AC-1.5).
  Verify by: `SetPinScreen.kt` + `SetPinViewModel` + test.
- [ ] **C12** Complete navigation: Welcome→Create→Verify→SetPIN→complete **and**
  Welcome→Import→SetPIN→complete; Set PIN back → Welcome (08 AC 17–19). Verify by:
  `OnboardingNavigation.kt` + `OnboardingRoutes.kt`.
- [ ] **C13** Navigation arguments match `SavedStateHandle` lookups (route string ↔ key names).
  Verify by: read routes + ViewModel `get<...>` calls.
- [ ] **C14** `@Preview` on all screens (07 AC 12, 08 AC 20). Verify by: `grep -c '@Preview'`
  per screen file.
- [ ] **C15** No `TODO()` (07 AC 14, 08 AC 21; E7 = 0).
- [ ] **C16** Mnemonic/PIN never logged (doc/08 Security). Verify by:
  `grep -nE 'Log\\.|println' feature/feature-onboarding/src/main`.
- [ ] **C17** ViewModel side effects in `init` blocks — pre-registered **2.0.2b** root cause;
  record every `init`-block call site for the developer session.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
