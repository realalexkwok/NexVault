# CR 1.7 — Onboarding (create, import, verify, set PIN)

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **fix round verified 2026-09-24 — both findings verified-fixed — ready to close on user
> confirmation** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.7 Onboarding (parts 1–2, Phase 0) |
| Prompts | `doc/prompts/07-Task-1.7-Part-1-...md` + `doc/prompts/08-Task-1.7-Part-2-...md` |
| Landing module | `feature:feature-onboarding` — 12 main + 5 test files |
| Tests | **30 pass** (fresh 2026-09-23 rerun) |
| `doc/08` anchor | AC-1.3 (wallet creation), AC-1.4 (import), AC-1.5 (PIN & biometric) |

## Measured file inventory (2026-09-21)

- Navigation: `OnboardingNavigation`, `OnboardingRoutes`.
- Screens: `WelcomeScreen`, `CreateWalletScreen`, `VerifyMnemonicScreen`, `ImportWalletScreen`,
  `SetPinScreen`.
- ViewModels: `WelcomeViewModel`, `CreateWalletViewModel`, `VerifyMnemonicViewModel`,
  `ImportWalletViewModel`, `SetPinViewModel`.
- Tests: one per ViewModel (5 files, 30 tests).
- Measure command: `find feature/feature-onboarding/src -name '*.kt' | wc -l` → 17.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b (BLOCKER):** `CreateWalletViewModel` calls `createWalletUseCase` from its `init`
  block, which persists the wallet **and** flips `is_wallet_set_up`; `NexVaultApp`'s
  `routingKey` (`NexVaultApp.kt:56,64,67`) tears down the NavHost mid-flow. Result on device:
  wallet created, **mnemonic never shown**, no PIN set, app unusable. Owner: **2.0.2b** (options
  A/B/C pending owner decision). This review records the exact code paths; it does not re-own it.
- `VerifyMnemonicViewModel` retrieves the mnemonic via `getMnemonicForBackup` (the
  unauthenticated chain from finding 1.6-1, deferred to 2.0.2b) — legitimate during onboarding,
  cross-referenced here.
- Prompt 08 AC 19: back from Set PIN must go to Welcome — implemented via `popUpTo(WELCOME)`.

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :feature:feature-onboarding:testDebugUnitTest --rerun-tasks` →
  **BUILD SUCCESSFUL, 30 tests, 0 failures, 0 skipped** (2026-09-23).
- [x] **C2** ✓ pass — Welcome: app name/tagline, Create/Import buttons emitting
  `NavigateToCreateWallet`/`NavigateToImportWallet` events.
- [x] **C3** ✓ pass (post-fix) — mnemonic grid now `GridCells.Fixed(4)` (4 columns × 3 rows)
  matching AC-1.3; KDoc corrected (1.7-2, verified fixed).
- [x] **C4** ✓ pass — acknowledge checkbox gates Continue (`enabled = uiState.isAcknowledged`).
- [x] **C5** ✓ pass — `FLAG_SECURE` set (and cleared on dispose) on `CreateWalletScreen` and
  `ImportWalletScreen`.
- [x] **C6** ✓ pass (post-fix) — `VerifyMnemonicScreen` now sets/clears `FLAG_SECURE` via
  `DisposableEffect` (same convention as Create/Import) (1.7-1, verified fixed).
- [x] **C7** ✓ pass — Verify: shuffled words, tap-in-order (`nextExpectedIndex`), `isVerified` on
  completion, error + reset on wrong selection.
- [x] **C8** ✓ pass — Import: Recovery Phrase ↔ Private Key mode toggle.
- [x] **C9** ✓ pass — real-time format validation per mode (word count / 64-hex) with error
  resources; full BIP-39 checksum validation happens at Import tap via the use case.
- [x] **C10** ✓ pass — import success → `NavigateToSetPin`; failure → error state.
- [x] **C11** ✓ pass — Set PIN: title, 6 digits, confirm phase with cleared input, mismatch shake +
  reset, biometric toggle rendered only when available + SET phase, biometric preference saved on
  store.
- [x] **C12** ✓ pass — complete routes for both paths; Set PIN back → Welcome via `popUpTo(WELCOME)`.
- [x] **C13** ✓ pass — `walletId` nav argument (`NavType.StringType`) matches the
  `SavedStateHandle` lookup key.
- [x] **C14** ✓ pass — `@Preview` on all 5 screens (Welcome 1, Create 1, Verify 1, Import 3, SetPin 3).
- [x] **C15** ✓ pass — `grep -rn 'TODO()' feature/feature-onboarding/src/main` → **0**.
- [x] **C16** ✓ pass — `grep -rnE 'Log\\.|println' feature/feature-onboarding/src/main` → **0**.
- [x] **C17** n/a → pre-registered **2.0.2b** — init-block side effects recorded exactly:
  `CreateWalletViewModel.init → createWallet()` (root cause), `VerifyMnemonicViewModel.init →
  loadMnemonic()`, `SetPinViewModel.init → checkBiometricAvailability()` (benign).

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(both findings fixed by the developer and verified by the reviewer 2026-09-24 — rows deleted; evidence in `../validation.md` §1.7)_ | | | | | | |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
