# CR 1.9 — Main scaffold & navigation shell

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **scan complete 2026-09-24 — 0 findings — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.9 Main scaffold & navigation shell (Phase 0) |
| Prompt | `doc/prompts/10-Task-1.9-Main-Scaffold-Navigation-Shell.md` |
| Landing module | `app` — `MainScreen`, `MainNavigation`, `MainTab`, `NexVaultApp`, `MainActivity`, `NexVaultApplication`, `di/AppNetworkModule` |
| Tests | 1 unit (template stub) + 1 instrumented (`BackupPolicyTest` 3 + `ExampleInstrumentedTest` 1-fail-known-1.1-3) |
| `doc/08` anchor | AC-1.7 (navigation) |

## Measured file inventory (2026-09-21)

- `ui/main/MainScreen.kt`, `navigation/MainNavigation.kt`, `navigation/MainTab.kt`,
  `NexVaultApp.kt`, `MainActivity.kt`, `NexVaultApplication.kt`, `di/AppNetworkModule.kt`
  (2.1's provider lives here), manifest, `ExampleUnitTest`, `ExampleInstrumentedTest`,
  `BackupPolicyTest` (1.1 fix round).

## Known notes (pre-registered findings — do not re-create)

- **Manifest entry point (FIXED, 2.0.2)** — see 1.1 C6; the app could never launch before.
- **4 of 5 tabs are placeholders** — real destinations are Phase 2.6–2.8 / 3.1–3.4 items.
- **Empty callbacks:** `MainScreen.kt:134–136` passes `onNavigateToSend = { }`,
  `onNavigateToReceive = { }`, `onNavigateToSwap = { }`. Owner: **2.0.3**.
- **ActionBar prediction REFUTED (4.14)** — `MainActivity` extends `FragmentActivity`.
- Prompt 10 AC 2 wanted 5 tabs (Home, History, DApp, NFT, Settings).

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :app:assembleDebug` **BUILD SUCCESSFUL** (2026-09-24; after a
  full fresh `--rerun-tasks` compile of the app module — see the environment note in
  `validation.md`).
- [x] **C2** ✓ pass — `MainTab` defines the 5 tabs (Home, History, DApp, NFT, Settings) with the
  correct order per doc/03 §2.3; Home is the start destination; 4 tabs render
  `PlaceholderTabScreen` (name + description).
- [x] **C3** ✓ pass — `NavigationBarItem.onClick` navigates with
  `popUpTo(findStartDestination) { saveState = true }` + `launchSingleTop = true` +
  `restoreState = true` — state preservation per prompt AC 4.
- [x] **C4** ✓ pass — filled/outlined icon pairs per tab, labels + contentDescriptions, selected
  state colored via `MaterialTheme.colorScheme.primary` (prompt AC 5–6).
- [x] **C5** ✓ pass (code half) — root flow (onboarding → main, unlock → main, auto-lock → unlock)
  verified in the 1.8 routing review; the device half is the 2.0.2 evidence (blocked at 2.0.2b for
  the full walk).
- [x] **C6** ✓ pass — 2 `@Preview`s on `MainScreen` + `PlaceholderTabScreenPreview`.
- [x] **C7** ✓ pass — `grep -rnE 'Color\\(0x' app/src/main` → **0**.
- [x] **C8** ✓ pass — `grep -rn 'TODO()' app/src/main` → **0**.
- [x] **C9** ✓ pass — `NexVaultApp.kt:70` uses `MainRoutes.MAIN_GRAPH`; `mainGraph()` registers
  the nested graph with `MainScreen` (old hardcoded graph replaced).
- [x] **C10** n/a → pre-registered **2.0.3** — the 4 placeholder tabs and the 3 empty callbacks are
  recorded exactly (`MainScreen.kt:134–136,156–184`), not re-created.
- [x] **C11** ✓ pass — `app` owns DI (`AppNetworkModule`), root navigation and the UI shell only —
  no business logic (verified in the 1.1 scan).
- [x] **C12** n/a → pre-registered **2.0.2b** — `routingKey` + `key(...)` teardown recorded,
  not re-created.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none — no findings; notes in `../validation.md` §1.9)_ | | | | | | |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
