# CR 1.9 — Main scaffold & navigation shell

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.9 Main scaffold & navigation shell (Phase 0) |
| Prompt | `doc/prompts/10-Task-1.9-Main-Scaffold-Navigation-Shell.md` |
| Landing module | `app` — `MainScreen`, `MainNavigation`, `MainTab`, `NexVaultApp`, `MainActivity`, `NexVaultApplication`, `di/AppNetworkModule` |
| Tests | 1 unit (template stub) + 1 instrumented (template stub) |
| `doc/08` anchor | AC-1.7 (navigation) |

## Measured file inventory (2026-09-21)

- `ui/main/MainScreen.kt`, `navigation/MainNavigation.kt`, `navigation/MainTab.kt`,
  `NexVaultApp.kt`, `MainActivity.kt`, `NexVaultApplication.kt`, `di/AppNetworkModule.kt`
  (2.1's provider lives here), manifest, `ExampleUnitTest`, `ExampleInstrumentedTest`.

## Known notes (pre-registered findings — do not re-create)

- **Manifest entry point (FIXED, 2.0.2)** — see 1.1 C6; the app could never launch before.
- **4 of 5 tabs are placeholders** — real destinations are Phase 2.6–2.8 / 3.1–3.4 items.
- **Empty callbacks:** `MainScreen` wires Home → TokenDetail but passes empty lambdas for send,
  receive, swap, history. Owner: **2.0.3**.
- **ActionBar prediction REFUTED (4.14)** — `MainActivity` extends `FragmentActivity`; no stray
  ActionBar on device (2.0.2 run).
- Prompt 10 AC 2 wanted 5 tabs (Home, History, DApp, NFT, Settings).

## Review checklist

- [ ] **C1** Build green. Verify by: E1 — `:app:assembleDebug` PASS (2026-09-21).
- [ ] **C2** Bottom bar: 5 tabs (Home, History, DApp, NFT, Settings), Home default, each tab shows a
  placeholder with name + description (prompt AC 2). Verify by: `MainScreen.kt` + `MainTab.kt`.
- [ ] **C3** Tab switching works; state preserved via `popUpTo(saveState) / launchSingleTop /
  restoreState` flags (AC 3–4). Verify by: `MainNavigation.kt` navigate calls.
- [ ] **C4** Correct icons + labels; selected tab visually distinct (AC 5–6). Verify by:
  `MainScreen` NavigationBar wiring.
- [ ] **C5** Root flow end-to-end (AC 7): onboarding → main; kill → unlock → main; auto-lock →
  unlock → main. Manual half — owner device run; automatic half is the 2.0.2 evidence.
- [ ] **C6** `@Preview` on composables (AC 8). Verify by: `grep -c '@Preview' app/src/main`.
- [ ] **C7** No hardcoded colors — MaterialTheme tokens only (AC 9). Verify by:
  `grep -nE 'Color\\(0x' app/src/main`.
- [ ] **C8** No `TODO()` (AC 10; E7 = 0).
- [ ] **C9** Old placeholder `main_graph` fully replaced; `MainRoutes.MAIN_GRAPH` used
  (AC 11–12). Verify by: `NexVaultApp.kt` + `MainNavigation.kt`.
- [ ] **C10** Placeholder tabs + empty callbacks — pre-registered Phase 2 items + **2.0.3**;
  record exactly which buttons/lambdas are empty for the developer session.
- [ ] **C11** `app` owns DI wiring + root navigation and nothing else of substance
  (tech-stack §2.6). Verify by: `di/AppNetworkModule.kt` scope + grep for business logic in `app`.
- [ ] **C12** `routingKey` teardown mechanism — pre-registered **2.0.2b** (option C territory);
  record its current shape for the developer session.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
