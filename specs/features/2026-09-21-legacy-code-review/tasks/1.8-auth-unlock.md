# CR 1.8 — Auth / unlock

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[ ]` not started — legend in `README.md`.
## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.8 Auth / unlock (Phase 0) |
| Prompt | `doc/prompts/09-Task-1.8-Auth-Unlock-Feature.md` |
| Landing module | `feature:feature-auth` — 3 main + 1 test file |
| Tests | **14 pass** |
| `doc/08` anchor | AC-1.5 (PIN & biometric), AC-1.6 (auto-lock), AC-1.7 (navigation routing) |

## Measured file inventory (2026-09-21)

- `navigation/AuthNavigation`, `screen/UnlockScreen`, `viewmodel/UnlockViewModel`,
  test `UnlockViewModelTest` (14 tests).
- Root routing + auto-lock live in `app`: `NexVaultApp.kt` (`routingKey` at lines 56/64/67),
  `MainActivity.kt`.
- Measure command: `find feature/feature-auth/src -name '*.kt' | wc -l` → 4.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b (BLOCKER):** the `routingKey` + `key(...)` teardown mechanism (also reviewed under 1.7)
  is the class of bug that strands the app at Unlock with a PIN that was never set. Owner: 2.0.2b
  (option C — drop the reactive root router — kills the whole class).
- Biometric pin `androidx.biometric 1.4.0-alpha07` — alpha-line decision pre-registered **4.10**.
- Auto-lock default 5 minutes (AC-1.6; configurable later — settings is 3.4).

## Review checklist

- [ ] **C1** Compiles + tests pass. Verify by: E2 — `feature-auth` 14 tests green.
- [ ] **C2** Unlock screen: app icon, "NexVault" title, PIN keypad (prompt AC 4).
  Verify by: `UnlockScreen.kt`.
- [ ] **C3** Correct 6-digit PIN → main; wrong PIN → error with remaining-attempts count
  (AC 5–6). Verify by: `UnlockViewModel` + `UnlockViewModelTest`.
- [ ] **C4** 5 wrong attempts → 30-second lockout with countdown, keypad disabled; expiry →
  re-enabled with fresh attempt counter (AC 7–9; AC-1.5). Verify by: `UnlockViewModel` +
  `SecurityPreferencesDataStore` (1.4 C5 cross-check).
- [ ] **C5** Biometric: auto-shows on load when enabled; "Use Biometric" re-triggers; cancel →
  PIN fallback without error (AC 10–13). Verify by: `UnlockScreen` biometric wiring +
  `BiometricHelper`.
- [ ] **C6** Root routing: no wallet → onboarding; wallet + not authenticated → auth; wallet +
  authenticated → main (AC 14; AC-1.7). Verify by: `NexVaultApp.kt` routing logic — the
  2.0.2b-pre-registered mechanism; record its current shape exactly.
- [ ] **C7** Auto-lock: background > `autoLockSeconds` → unlock on return; shorter → main
  (AC 15–16; AC-1.6). Verify by: `NexVaultApp` onStop/onStart timestamp logic + `AppStateManager`.
- [ ] **C8** `MainActivity` extends `FragmentActivity` (AC 20). Verify by: `MainActivity.kt`.
- [ ] **C9** PIN verification path uses the hashed store (no plaintext compare).
  Verify by: `UnlockViewModel` → `VerifyPinUseCase` → `AuthRepository` chain.
- [ ] **C10** `@Preview` present; no `TODO()` (AC 18–19; E7 = 0).
- [ ] **C11** Lockout countdown survives process death or is re-derived from stored timestamps
  (AC-1.5 robustness). Verify by: `SecurityPreferencesDataStore` timestamp fields.

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none yet)_ | | | | | | |
## Exit criteria

- Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- Owner sign-off recorded in `../validation.md` with the date.
