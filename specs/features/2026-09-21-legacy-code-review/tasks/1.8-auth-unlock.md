# CR 1.8 — Auth / unlock

> Legacy code review task. Rules: `../requirements.md` · method: `../plan.md` · evidence: `../validation.md`.
> Reviewer role: production code is **read-only**; findings are recorded here only (Q3/Q5).
> Status: `[?]` **scan complete 2026-09-24 — 0 findings — awaiting sign-off** — legend in `README.md`.

## Scope

| Field | Value |
| --- | --- |
| Roadmap item | 1.8 Auth / unlock (Phase 0) |
| Prompt | `doc/prompts/09-Task-1.8-Auth-Unlock-Feature.md` |
| Landing module | `feature:feature-auth` — 3 main + 1 test file |
| Tests | **14 pass** (fresh 2026-09-24 rerun) |
| `doc/08` anchor | AC-1.5 (PIN & biometric), AC-1.6 (auto-lock), AC-1.7 (navigation routing) |

## Measured file inventory (2026-09-21)

- `navigation/AuthNavigation`, `screen/UnlockScreen`, `viewmodel/UnlockViewModel`,
  test `UnlockViewModelTest` (14 tests).
- Root routing + auto-lock live in `app`: `NexVaultApp.kt` (`routingKey` at lines 56/64/67),
  `MainActivity.kt` (`onStop`/`onStart` timestamp logic).
- Measure command: `find feature/feature-auth/src -name '*.kt' | wc -l` → 4.

## Known notes (pre-registered findings — do not re-create)

- **2.0.2b (BLOCKER):** the `routingKey` + `key(...)` teardown mechanism (also reviewed under 1.7)
  is the class of bug that strands the app at Unlock with a PIN that was never set. Owner: 2.0.2b
  (option C — drop the reactive root router — kills the whole class).
- Biometric pin `androidx.biometric 1.4.0-alpha07` — alpha-line decision pre-registered **4.10**.
- Auto-lock default 5 minutes (AC-1.6; configurable later — settings is 3.4).

## Review checklist

- [x] **C1** ✓ pass — `./gradlew :feature:feature-auth:testDebugUnitTest :feature:feature-auth:assembleDebug
  --rerun-tasks` → **BUILD SUCCESSFUL, 14 tests, 0 failures, 0 skipped** (2026-09-24).
- [x] **C2** ✓ pass — UnlockScreen: app icon (logo, `logoSizeSmall`), title, PIN keypad.
- [x] **C3** ✓ pass — correct PIN → `NavigateToMain`; wrong PIN → error with remaining-attempts
  count (`MAX_ATTEMPTS - failedAttempts` + plurals resource).
- [x] **C4** ✓ pass (noted) — 5 wrong attempts → 30 s countdown, keypad disabled via `isLockedOut`;
  expiry re-enables and resets the **VM's local** `failedAttempts` display. Note: the repository
  ladder (AppStateManager) stays cumulative per the 1.4-1 owner decision, so one failure after expiry
  re-locks for 30 s while the UI shows a reset counter — recorded as a reviewer note, not a finding.
- [x] **C5** ✓ pass — biometric: auto-show on load when enabled (`checkBiometricStatus` → emits
  `ShowBiometricPrompt`), "Use Biometric" re-triggers (`onBiometricRequested`), cancel/error →
  PIN fallback without error.
- [x] **C6** ✓ pass — root routing: `routingKey = remember(isWalletSetUp, isAuthed)` →
  onboarding / auth / main; the `key(routingKey)` teardown is the 2.0.2b pre-registered mechanism
  (recorded, not re-created).
- [x] **C7** ✓ pass — auto-lock: `MainActivity.onStop` stamps `backgroundedAt` (only when
  authenticated); `onStart` compares elapsed vs `autoLockTimeout` (NEVER → never, IMMEDIATE → 0,
  default 5 min from prefs) and flips `isAuthenticated`; process death re-locks naturally (state
  not persisted).
- [x] **C8** ✓ pass — `MainActivity` extends `FragmentActivity` (prompt AC 20).
- [x] **C9** ✓ pass — PIN verified through `VerifyPinUseCase` → `AuthRepositoryImpl` →
  `SecurityUtils.verifyPassword` (constant-time, hashed store) — chain verified across 1.4/1.6.
- [x] **C10** ✓ pass — 4 `@Preview`s on `UnlockScreen`; `grep TODO()` → 0; `grep Log\\.|println` → 0.
- [x] **C11** ✓ pass — lockout survives process death via stored `lockout_end_time`
  (`AppStateManager.isLockedOut`/countdown re-derive from the timestamp — verified in 1.4).

## Findings

> Transfer channel to the developer session. A row is deleted by the reviewer only after the
developer reports the fix and the reviewer verifies it (`requirements.md` Q6).

| ID | Severity | Location | Finding | Evidence | Owning roadmap item | Status |
| --- | --- | --- | --- | --- | --- | --- |
| _(none — no findings; reviewer notes in `../validation.md` §1.8)_ | | | | | | |

## Exit criteria

- [x] Every checklist item carries a verdict (`✓ pass` / `✗ finding` / `n/a` + reason).
- [x] Every `✗` verdict has a finding row with evidence and an owning roadmap item.
- [ ] Sign-off recorded in `../validation.md` with the date.
